import { l as ApiError, s as useRole } from "./role-context-YJO8G-xL.js";
import { a as AppHeader, i as CardTitle, n as CardContent, o as Badge, r as CardHeader, t as Card } from "./card-eBpIeksT.js";
import { t as Button } from "./button-PJVP9td7.js";
import { t as Input } from "./input-B8Q2ztVi.js";
import { t as Label } from "./label-DBD1bRRP.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-Dg1urBTx.js";
import { n as createBooking, r as getBookings, s as returnBooking } from "./bookings-BMnMGXf-.js";
import { a as DialogTitle, i as DialogHeader, n as DialogContent, o as DialogTrigger, r as DialogFooter, t as Dialog } from "./dialog-B8mBdC_P.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-C0WYWEQX.js";
import { i as getResources, n as createResource, r as deleteResource } from "./resources-CdRZ6Kye.js";
import { useMemo, useState } from "react";
import { Navigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { BookMarked, BookOpen, Hash, Layers, Library, Plus, Search, Trash2 } from "lucide-react";
//#region src/routes/app.library.tsx?tsr-split=component
var CATEGORIES = [
	"Academic Report",
	"Textbook",
	"Novel",
	"Reference"
];
var LEVELS = [
	"L1",
	"L2",
	"L3",
	"M1",
	"M2",
	"PhD",
	"General"
];
function dueDate(days) {
	const d = /* @__PURE__ */ new Date();
	d.setDate(d.getDate() + days);
	return d.toISOString().slice(0, 10);
}
var STATUS_TONE = {
	pending: "border-warning/40 text-warning",
	approved: "border-primary/40 text-primary",
	checked_out: "border-primary/40 text-primary",
	returned: "border-success/40 text-success",
	overdue: "",
	rejected: "",
	cancelled: ""
};
function LibraryPage() {
	const { user, can } = useRole();
	if (!can("nav:library")) return /* @__PURE__ */ jsx(Navigate, { to: "/app/dashboard" });
	if (!user) return null;
	const canManage = can("action:manageLibrary");
	const canDelete = user.role === "admin";
	const queryClient = useQueryClient();
	const { data: books = [], isLoading: loadingBooks } = useQuery({
		queryKey: ["resources", "book"],
		queryFn: () => getResources({ type: "book" })
	});
	const { data: loans = [], isLoading: loadingLoans } = useQuery({
		queryKey: [
			"bookings",
			"book",
			canManage ? "checked_out" : "mine"
		],
		queryFn: () => canManage ? getBookings({
			status: "checked_out",
			resourceType: "book"
		}) : getBookings({ resourceType: "book" })
	});
	const visibleLoans = canManage ? loans : loans.filter((l) => !["cancelled", "rejected"].includes(l.status));
	const refresh = () => {
		queryClient.invalidateQueries({ queryKey: ["resources", "book"] });
		queryClient.invalidateQueries({ queryKey: ["bookings", "book"] });
	};
	const [q, setQ] = useState("");
	const [field, setField] = useState("all");
	const [category, setCategory] = useState("all");
	const [level, setLevel] = useState("all");
	const filtered = useMemo(() => {
		const needle = q.trim().toLowerCase();
		return books.filter((b) => {
			if (category !== "all" && b.category !== category) return false;
			if (level !== "all" && b.level !== level) return false;
			if (!needle) return true;
			return {
				all: `${b.name} ${b.author ?? ""} ${b.isbn ?? ""} ${b.speciality ?? ""} ${b.level ?? ""} ${b.category ?? ""}`,
				title: b.name,
				author: b.author ?? "",
				isbn: b.isbn ?? "",
				speciality: b.speciality ?? "",
				level: b.level ?? ""
			}[field].toLowerCase().includes(needle);
		});
	}, [
		books,
		q,
		field,
		category,
		level
	]);
	const metrics = useMemo(() => {
		const total = books.reduce((a, b) => a + (b.totalCopies ?? 0), 0);
		const available = books.reduce((a, b) => a + (b.availableCopies ?? 0), 0);
		return {
			total,
			available,
			onLoan: total - available,
			reports: books.filter((b) => b.category === "Academic Report").length,
			titles: books.length
		};
	}, [books]);
	const [open, setOpen] = useState(false);
	const empty = {
		title: "",
		author: "",
		isbn: "",
		category: "Textbook",
		speciality: "",
		level: "L1",
		location: "Central Library",
		copiesTotal: 1
	};
	const [form, setForm] = useState(empty);
	const [saving, setSaving] = useState(false);
	const addBook = async () => {
		if (!form.title || !form.author || !form.isbn) {
			toast.error("Title, author and ISBN are required");
			return;
		}
		setSaving(true);
		try {
			await createResource({
				name: form.title,
				author: form.author,
				isbn: form.isbn,
				type: "book",
				category: form.category,
				speciality: form.speciality,
				level: form.level,
				location: form.location,
				quantity: form.copiesTotal,
				totalCopies: form.copiesTotal,
				availableCopies: form.copiesTotal
			});
			toast.success(`"${form.title}" added to catalog`);
			setForm(empty);
			setOpen(false);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to add catalog entry");
		} finally {
			setSaving(false);
		}
	};
	const removeBook = async (b) => {
		try {
			await deleteResource(b._id);
			toast.success("Entry removed");
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to remove entry");
		}
	};
	const [borrowingId, setBorrowingId] = useState(null);
	const borrow = async (b) => {
		if ((b.availableCopies ?? 0) < 1) {
			toast.error("No copies available");
			return;
		}
		setBorrowingId(b._id);
		try {
			await createBooking({
				resource: b._id,
				dueDate: dueDate(14),
				purpose: "Library loan request"
			});
			toast.success(`Borrow request submitted · "${b.name}"`, { description: "Collect at the circulation desk once approved." });
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to submit borrow request");
		} finally {
			setBorrowingId(null);
		}
	};
	const [returningId, setReturningId] = useState(null);
	const checkin = async (loan) => {
		setReturningId(loan._id);
		try {
			await returnBooking(loan._id);
			toast.success(`Check-in recorded · ${loan.resource?.name ?? "item"}`);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Check-in failed");
		} finally {
			setReturningId(null);
		}
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "Library Catalog",
		subtitle: canManage ? "Books · academic reports · novels — circulation & inventory" : "Search books, reports and novels — borrow with one click"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-5 lg:px-8 py-6 space-y-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-2 lg:grid-cols-4 gap-4",
				children: [
					/* @__PURE__ */ jsx(Metric, {
						icon: Library,
						label: "Titles in catalog",
						value: loadingBooks ? "…" : `${metrics.titles}`
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: BookOpen,
						label: "Copies available",
						value: loadingBooks ? "…" : `${metrics.available} / ${metrics.total}`
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: BookMarked,
						label: "Copies on loan",
						value: loadingBooks ? "…" : `${metrics.onLoan}`
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: Hash,
						label: "Academic reports",
						value: loadingBooks ? "…" : `${metrics.reports}`
					})
				]
			}),
			/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, {
				className: "flex flex-row items-center justify-between flex-wrap gap-3",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs(CardTitle, {
					className: "text-base flex items-center gap-2",
					children: [/* @__PURE__ */ jsx(Search, { className: "h-4 w-4 text-primary" }), " Catalog search"]
				}), /* @__PURE__ */ jsx("p", {
					className: "text-xs text-muted-foreground",
					children: "Filter by title, author, ISBN, speciality or class level"
				})] }), canManage && /* @__PURE__ */ jsxs(Dialog, {
					open,
					onOpenChange: setOpen,
					children: [/* @__PURE__ */ jsx(DialogTrigger, {
						asChild: true,
						children: /* @__PURE__ */ jsxs(Button, {
							size: "sm",
							className: "gap-1.5",
							children: [/* @__PURE__ */ jsx(Plus, { className: "h-3.5 w-3.5" }), " Add book"]
						})
					}), /* @__PURE__ */ jsxs(DialogContent, {
						className: "sm:max-w-lg",
						children: [
							/* @__PURE__ */ jsx(DialogHeader, { children: /* @__PURE__ */ jsx(DialogTitle, { children: "Add a catalog entry" }) }),
							/* @__PURE__ */ jsxs("div", {
								className: "grid grid-cols-2 gap-3 py-2",
								children: [
									/* @__PURE__ */ jsx(Field, {
										label: "Title",
										wide: true,
										children: /* @__PURE__ */ jsx(Input, {
											value: form.title,
											onChange: (e) => setForm({
												...form,
												title: e.target.value
											})
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Author",
										children: /* @__PURE__ */ jsx(Input, {
											value: form.author,
											onChange: (e) => setForm({
												...form,
												author: e.target.value
											})
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "ISBN / Ref",
										children: /* @__PURE__ */ jsx(Input, {
											value: form.isbn,
											onChange: (e) => setForm({
												...form,
												isbn: e.target.value
											}),
											placeholder: "978-... or REP-..."
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Category",
										children: /* @__PURE__ */ jsxs(Select, {
											value: form.category,
											onValueChange: (v) => setForm({
												...form,
												category: v
											}),
											children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsx(SelectContent, { children: CATEGORIES.map((c) => /* @__PURE__ */ jsx(SelectItem, {
												value: c,
												children: c
											}, c)) })]
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Class / Level",
										children: /* @__PURE__ */ jsxs(Select, {
											value: form.level,
											onValueChange: (v) => setForm({
												...form,
												level: v
											}),
											children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsx(SelectContent, { children: LEVELS.map((l) => /* @__PURE__ */ jsx(SelectItem, {
												value: l,
												children: l
											}, l)) })]
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Speciality",
										wide: true,
										children: /* @__PURE__ */ jsx(Input, {
											value: form.speciality,
											onChange: (e) => setForm({
												...form,
												speciality: e.target.value
											}),
											placeholder: "Computer Science / EE / …"
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Location",
										children: /* @__PURE__ */ jsx(Input, {
											value: form.location,
											onChange: (e) => setForm({
												...form,
												location: e.target.value
											})
										})
									}),
									/* @__PURE__ */ jsx(Field, {
										label: "Copies",
										children: /* @__PURE__ */ jsx(Input, {
											type: "number",
											min: 1,
											value: form.copiesTotal,
											onChange: (e) => setForm({
												...form,
												copiesTotal: Math.max(1, Number(e.target.value))
											})
										})
									})
								]
							}),
							/* @__PURE__ */ jsx(DialogFooter, { children: /* @__PURE__ */ jsx(Button, {
								onClick: addBook,
								disabled: saving,
								children: saving ? "Adding…" : "Add to catalog"
							}) })
						]
					})]
				})]
			}), /* @__PURE__ */ jsxs(CardContent, {
				className: "space-y-3",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "grid grid-cols-1 md:grid-cols-[1fr_180px_200px_140px] gap-2",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "relative",
							children: [/* @__PURE__ */ jsx(Search, { className: "absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" }), /* @__PURE__ */ jsx(Input, {
								value: q,
								onChange: (e) => setQ(e.target.value),
								placeholder: "Search title, author, ISBN, speciality, class…",
								className: "pl-9"
							})]
						}),
						/* @__PURE__ */ jsxs(Select, {
							value: field,
							onValueChange: (v) => setField(v),
							children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsxs(SelectContent, { children: [
								/* @__PURE__ */ jsx(SelectItem, {
									value: "all",
									children: "All fields"
								}),
								/* @__PURE__ */ jsx(SelectItem, {
									value: "title",
									children: "Title"
								}),
								/* @__PURE__ */ jsx(SelectItem, {
									value: "author",
									children: "Author"
								}),
								/* @__PURE__ */ jsx(SelectItem, {
									value: "isbn",
									children: "ISBN / Ref"
								}),
								/* @__PURE__ */ jsx(SelectItem, {
									value: "speciality",
									children: "Speciality"
								}),
								/* @__PURE__ */ jsx(SelectItem, {
									value: "level",
									children: "Class / Level"
								})
							] })]
						}),
						/* @__PURE__ */ jsxs(Select, {
							value: category,
							onValueChange: (v) => setCategory(v),
							children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
								value: "all",
								children: "All categories"
							}), CATEGORIES.map((c) => /* @__PURE__ */ jsx(SelectItem, {
								value: c,
								children: c
							}, c))] })]
						}),
						/* @__PURE__ */ jsxs(Select, {
							value: level,
							onValueChange: setLevel,
							children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
								value: "all",
								children: "All levels"
							}), LEVELS.map((l) => /* @__PURE__ */ jsx(SelectItem, {
								value: l,
								children: l
							}, l))] })]
						})
					]
				}), /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
					/* @__PURE__ */ jsx(TableHead, { children: "Ref" }),
					/* @__PURE__ */ jsx(TableHead, { children: "Title" }),
					/* @__PURE__ */ jsx(TableHead, { children: "Author" }),
					/* @__PURE__ */ jsx(TableHead, { children: "ISBN / Ref" }),
					/* @__PURE__ */ jsx(TableHead, { children: "Category" }),
					/* @__PURE__ */ jsx(TableHead, { children: "Speciality · Level" }),
					/* @__PURE__ */ jsx(TableHead, {
						className: "text-right",
						children: "Avail"
					}),
					/* @__PURE__ */ jsx(TableHead, {
						className: "text-right",
						children: "Actions"
					})
				] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [
					loadingBooks && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 8,
						className: "text-center text-muted-foreground py-6",
						children: "Loading…"
					}) }),
					!loadingBooks && filtered.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 8,
						className: "text-center text-muted-foreground py-6",
						children: "No catalog entries match."
					}) }),
					filtered.map((b) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-[11px]",
							children: b._id.slice(-6)
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-medium max-w-[260px] truncate",
							title: b.name,
							children: b.name
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-xs",
							children: b.author
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-[11px]",
							children: b.isbn
						}),
						/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
							variant: "outline",
							className: "text-[10px]",
							children: b.category
						}) }),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "text-xs",
							children: [
								/* @__PURE__ */ jsx(Layers, { className: "inline h-3 w-3 mr-1 text-muted-foreground" }),
								b.speciality,
								" · ",
								/* @__PURE__ */ jsx("span", {
									className: "font-mono",
									children: b.level
								})
							]
						}),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "text-right tabular-nums",
							children: [
								/* @__PURE__ */ jsx("span", {
									className: b.availableCopies === 0 ? "text-destructive" : b.availableCopies < 2 ? "text-warning" : "text-success",
									children: b.availableCopies
								}),
								" / ",
								b.totalCopies
							]
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-right",
							children: /* @__PURE__ */ jsxs("div", {
								className: "inline-flex gap-1",
								children: [/* @__PURE__ */ jsx(Button, {
									size: "sm",
									variant: "outline",
									disabled: (b.availableCopies ?? 0) < 1 || borrowingId === b._id,
									onClick: () => borrow(b),
									children: borrowingId === b._id ? "…" : "Borrow"
								}), canDelete && /* @__PURE__ */ jsx(Button, {
									size: "icon",
									variant: "ghost",
									className: "h-7 w-7 text-destructive",
									onClick: () => removeBook(b),
									children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
								})]
							})
						})
					] }, b._id))
				] })] })]
			})] }),
			/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
				className: "text-base",
				children: canManage ? "Checkout list · borrowed articles" : "My checkouts"
			}), /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground",
				children: canManage ? "Every book, report or novel currently out flows through this list" : "Books currently issued or requested on your account"
			})] }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableHead, { children: "Loan" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Title" }),
				canManage && /* @__PURE__ */ jsx(TableHead, { children: "Borrower" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Due / Status" }),
				/* @__PURE__ */ jsx(TableHead, {
					className: "text-right",
					children: "Action"
				})
			] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [
				loadingLoans && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
					colSpan: canManage ? 5 : 4,
					className: "text-center text-muted-foreground py-6",
					children: "Loading…"
				}) }),
				!loadingLoans && visibleLoans.map((l) => {
					const overdue = l.status === "checked_out" && l.dueDate && new Date(l.dueDate) < /* @__PURE__ */ new Date();
					return /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-xs",
							children: l._id.slice(-6)
						}),
						/* @__PURE__ */ jsx(TableCell, { children: l.resource?.name ?? "—" }),
						canManage && /* @__PURE__ */ jsxs(TableCell, { children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm",
							children: l.user?.name
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[10px] text-muted-foreground font-mono",
							children: l.user?.matricule ?? l.user?.email
						})] }),
						/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
							variant: overdue ? "destructive" : "outline",
							className: !overdue ? STATUS_TONE[l.status] ?? "" : "",
							children: l.status === "checked_out" && l.dueDate ? `${l.dueDate.slice(0, 10)}${overdue ? " · overdue" : ""}` : l.status.replace(/_/g, " ")
						}) }),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-right",
							children: canManage && l.status === "checked_out" && /* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "outline",
								disabled: returningId === l._id,
								onClick: () => checkin(l),
								children: returningId === l._id ? "…" : "Check in"
							})
						})
					] }, l._id);
				}),
				!loadingLoans && visibleLoans.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
					colSpan: canManage ? 5 : 4,
					className: "text-center text-muted-foreground py-6",
					children: "No active loans."
				}) })
			] })] }) })] })
		]
	})] });
}
function Metric({ icon: Icon, label, value }) {
	return /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
		className: "p-4 flex items-center gap-3",
		children: [/* @__PURE__ */ jsx("div", {
			className: "h-10 w-10 rounded-lg bg-primary/15 ring-1 ring-primary/30 flex items-center justify-center",
			children: /* @__PURE__ */ jsx(Icon, { className: "h-4.5 w-4.5 text-primary" })
		}), /* @__PURE__ */ jsxs("div", {
			className: "min-w-0",
			children: [/* @__PURE__ */ jsx("p", {
				className: "text-[11px] uppercase tracking-wider text-muted-foreground",
				children: label
			}), /* @__PURE__ */ jsx("p", {
				className: "text-lg font-semibold leading-tight",
				children: value
			})]
		})]
	}) });
}
function Field({ label, wide, children }) {
	return /* @__PURE__ */ jsxs("div", {
		className: `grid gap-1.5 ${wide ? "col-span-2" : ""}`,
		children: [/* @__PURE__ */ jsx(Label, { children: label }), children]
	});
}
//#endregion
export { LibraryPage as component };
