import { d as apiRequest } from "./role-context-YJO8G-xL.js";
//#region src/lib/api/academic.ts
var getCourses = () => apiRequest("/courses");
var createCourse = (input) => apiRequest("/courses", {
	method: "POST",
	body: input
});
var enrollStudent = (courseId, studentId) => apiRequest(`/courses/${courseId}/enroll`, {
	method: "PUT",
	body: { studentId }
});
var unenrollStudent = (courseId, studentId) => apiRequest(`/courses/${courseId}/unenroll`, {
	method: "PUT",
	body: { studentId }
});
var getAttendance = () => apiRequest("/attendance");
var getAttendanceByCourse = (courseId) => apiRequest(`/attendance/course/${courseId}`);
var createAttendance = (input) => apiRequest("/attendance", {
	method: "POST",
	body: input
});
var markAttendance = (id, input) => apiRequest(`/attendance/${id}/mark`, {
	method: "PUT",
	body: input
});
var getResults = () => apiRequest("/results");
var getResultsByCourse = (courseId) => apiRequest(`/results/course/${courseId}`);
var createResult = (input) => apiRequest("/results", {
	method: "POST",
	body: input
});
var addAssessmentResult = (id, input) => apiRequest(`/results/${id}/assessment`, {
	method: "PUT",
	body: input
});
var updateAssessmentResult = (id, assessmentId, input) => apiRequest(`/results/${id}/assessment/${assessmentId}`, {
	method: "PUT",
	body: input
});
//#endregion
export { enrollStudent as a, getCourses as c, markAttendance as d, unenrollStudent as f, createResult as i, getResults as l, createAttendance as n, getAttendance as o, updateAssessmentResult as p, createCourse as r, getAttendanceByCourse as s, addAssessmentResult as t, getResultsByCourse as u };
