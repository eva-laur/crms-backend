import { l as ApiError, s as useRole } from "./role-context-YJO8G-xL.js";
import { a as AppHeader, i as CardTitle, n as CardContent, o as Badge, r as CardHeader, t as Card } from "./card-eBpIeksT.js";
import { t as Button } from "./button-PJVP9td7.js";
import { t as Input } from "./input-B8Q2ztVi.js";
import { t as Label } from "./label-DBD1bRRP.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-Dg1urBTx.js";
import { c as updateBookingStatus, n as createBooking, r as getBookings, s as returnBooking, t as checkOutBooking } from "./bookings-BMnMGXf-.js";
import { a as DialogTitle, i as DialogHeader, n as DialogContent, o as DialogTrigger, r as DialogFooter, t as Dialog } from "./dialog-B8mBdC_P.js";
import { i as TabsTrigger, n as TabsContent, r as TabsList, t as Tabs } from "./tabs-CCJRliUM.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-C0WYWEQX.js";
import { a as setResourceMaintenance, i as getResources, n as createResource, r as deleteResource, t as clearResourceMaintenance } from "./resources-CdRZ6Kye.js";
import { useMemo, useState } from "react";
import { Navigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Cable, Camera, Headphones, Laptop, Mic, PackageCheck, Plus, Projector, Search, Tablet, Trash2, Tv, Wrench } from "lucide-react";
import { Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
//#region src/routes/app.it-equipment.tsx?tsr-split=component
var CAT_ICON = {
	Projector,
	Camera,
	Laptop,
	Microphone: Mic,
	Display: Tv,
	Tablet,
	Headset: Headphones,
	Cabling: Cable
};
var CAT_COLOR = {
	Projector: "oklch(0.62 0.16 155)",
	Camera: "oklch(0.82 0.16 88)",
	Laptop: "oklch(0.55 0.14 200)",
	Microphone: "oklch(0.68 0.18 30)",
	Display: "oklch(0.62 0.17 280)",
	Tablet: "oklch(0.72 0.14 160)",
	Headset: "oklch(0.55 0.10 100)",
	Cabling: "oklch(0.50 0.05 240)"
};
var CATEGORIES = [
	"Projector",
	"Camera",
	"Laptop",
	"Microphone",
	"Display",
	"Tablet",
	"Headset",
	"Cabling"
];
var CONDITIONS = [
	"Excellent",
	"Good",
	"Worn",
	"Faulty"
];
function dueIso(days) {
	const d = /* @__PURE__ */ new Date();
	d.setDate(d.getDate() + days);
	return d.toISOString().slice(0, 10);
}
function ITPage() {
	const { user, can } = useRole();
	if (!can("nav:itequipment")) return /* @__PURE__ */ jsx(Navigate, { to: "/app/dashboard" });
	if (!user) return null;
	const canManage = can("action:manageIT");
	const canDelete = user.role === "admin";
	const queryClient = useQueryClient();
	const { data: items = [], isLoading: loadingItems } = useQuery({
		queryKey: ["resources", "equipment"],
		queryFn: () => getResources({ type: "equipment" })
	});
	const { data: allBookings = [], isLoading: loadingBookings } = useQuery({
		queryKey: [
			"bookings",
			"equipment",
			"all"
		],
		queryFn: () => getBookings({ resourceType: "equipment" })
	});
	const checkouts = allBookings.filter((b) => b.status === "checked_out");
	const requests = allBookings.filter((b) => [
		"pending",
		"approved",
		"rejected"
	].includes(b.status));
	const refresh = () => {
		queryClient.invalidateQueries({ queryKey: ["resources", "equipment"] });
		queryClient.invalidateQueries({ queryKey: ["bookings", "equipment"] });
	};
	const displayStatus = (it) => {
		if (it.status === "under_maintenance") return "maintenance";
		if (checkouts.some((c) => (c.resource?._id ?? c.resource) === it._id)) return "checked_out";
		return "available";
	};
	const [q, setQ] = useState("");
	const [cat, setCat] = useState("all");
	const [status, setStatus] = useState("all");
	const filtered = useMemo(() => items.filter((it) => {
		if (cat !== "all" && it.category !== cat) return false;
		if (status !== "all" && displayStatus(it) !== status) return false;
		const needle = q.trim().toLowerCase();
		if (!needle) return true;
		return `${it.assetTag ?? ""} ${it.name} ${it.brand ?? ""} ${it.category ?? ""} ${it.location ?? ""}`.toLowerCase().includes(needle);
	}), [
		items,
		q,
		cat,
		status,
		checkouts
	]);
	const metrics = useMemo(() => ({
		total: items.reduce((a, i) => a + (i.quantity ?? 0), 0),
		available: items.filter((i) => displayStatus(i) === "available").reduce((a, i) => a + (i.quantity ?? 0), 0),
		out: checkouts.length,
		mx: items.filter((i) => i.status === "under_maintenance").length,
		pending: requests.filter((r) => r.status === "pending").length
	}), [
		items,
		checkouts,
		requests
	]);
	const usageData = useMemo(() => {
		const acc = {};
		for (const c of checkouts) {
			const cat = c.resource?.category;
			if (!cat) continue;
			acc[cat] = (acc[cat] ?? 0) + 1;
		}
		if (Object.keys(acc).length === 0) {
			for (const it of items) if (displayStatus(it) === "checked_out") acc[it.category] = (acc[it.category] ?? 0) + 1;
		}
		return Object.entries(acc).map(([name, value]) => ({
			name,
			value
		}));
	}, [items, checkouts]);
	const empty = {
		assetTag: "",
		name: "",
		category: "Projector",
		brand: "",
		condition: "Excellent",
		location: "",
		quantity: 1
	};
	const [open, setOpen] = useState(false);
	const [form, setForm] = useState(empty);
	const [saving, setSaving] = useState(false);
	const addItem = async () => {
		if (!form.assetTag || !form.name) {
			toast.error("Asset tag and name required");
			return;
		}
		setSaving(true);
		try {
			await createResource({
				name: form.name,
				assetTag: form.assetTag,
				type: "equipment",
				category: form.category,
				brand: form.brand,
				condition: form.condition,
				location: form.location,
				quantity: form.quantity
			});
			toast.success(`${form.assetTag} added (${form.quantity} unit${form.quantity > 1 ? "s" : ""})`);
			setForm(empty);
			setOpen(false);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to add item");
		} finally {
			setSaving(false);
		}
	};
	const [requestingId, setRequestingId] = useState(null);
	const requestItem = async (it) => {
		setRequestingId(it._id);
		try {
			await createBooking({
				resource: it._id,
				dueDate: dueIso(3),
				purpose: "User request"
			});
			toast.success(`Request submitted for ${it.assetTag ?? it.name}`);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to submit request");
		} finally {
			setRequestingId(null);
		}
	};
	const [decidingId, setDecidingId] = useState(null);
	const decide = async (r, decision) => {
		setDecidingId(r._id);
		try {
			await updateBookingStatus(r._id, decision);
			if (decision === "approved") await checkOutBooking(r._id);
			toast.success(`Request ${decision}`);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to update request");
		} finally {
			setDecidingId(null);
		}
	};
	const [returningId, setReturningId] = useState(null);
	const checkin = async (c) => {
		setReturningId(c._id);
		try {
			await returnBooking(c._id);
			toast.success(`Returned · ${c.resource?.assetTag ?? c.resource?.name ?? "item"}`);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Return failed");
		} finally {
			setReturningId(null);
		}
	};
	const setItemStatus = async (it, next) => {
		try {
			if (next === "maintenance") {
				const note = window.prompt(`Maintenance note for ${it.assetTag ?? it.name}:`, "Under maintenance");
				if (note === null) return;
				await setResourceMaintenance(it._id, note);
			} else await clearResourceMaintenance(it._id);
			toast.success(`${it.assetTag ?? it.name} → ${next === "maintenance" ? "under maintenance" : "available"}`);
			refresh();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to update status");
		}
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "IT Equipment Inventory",
		subtitle: canManage ? "Projectors · cameras · laptops · mics · displays — issue & track" : "Browse available equipment and submit a request"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-3 sm:px-5 lg:px-8 py-6 space-y-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-2 lg:grid-cols-5 gap-3",
				children: [
					/* @__PURE__ */ jsx(Metric, {
						icon: Laptop,
						label: "Units tracked",
						value: loadingItems ? "…" : `${metrics.total}`
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: Cable,
						label: "Available",
						value: loadingItems ? "…" : `${metrics.available}`,
						tone: "success"
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: Camera,
						label: "Active checkouts",
						value: loadingBookings ? "…" : `${metrics.out}`,
						tone: "primary"
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: Wrench,
						label: "In maintenance",
						value: loadingItems ? "…" : `${metrics.mx}`,
						tone: "warning"
					}),
					/* @__PURE__ */ jsx(Metric, {
						icon: Mic,
						label: "Pending requests",
						value: loadingBookings ? "…" : `${metrics.pending}`,
						tone: "warning"
					})
				]
			}),
			canManage && /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
				className: "text-base",
				children: "Equipment usage by category"
			}), /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground",
				children: "Share of current checkouts across hardware types"
			})] }), /* @__PURE__ */ jsx(CardContent, { children: usageData.length === 0 ? /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground text-center py-10",
				children: "No active checkouts to chart yet."
			}) : /* @__PURE__ */ jsx("div", {
				className: "h-72",
				children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(PieChart, { children: [
					/* @__PURE__ */ jsx(Pie, {
						data: usageData,
						dataKey: "value",
						nameKey: "name",
						innerRadius: 55,
						outerRadius: 95,
						paddingAngle: 3,
						children: usageData.map((d) => /* @__PURE__ */ jsx(Cell, {
							fill: CAT_COLOR[d.name] ?? "oklch(0.55 0.05 160)",
							stroke: "none"
						}, d.name))
					}),
					/* @__PURE__ */ jsx(Tooltip, {}),
					/* @__PURE__ */ jsx(Legend, {
						iconType: "circle",
						wrapperStyle: { fontSize: 11 }
					})
				] }) })
			}) })] }),
			/* @__PURE__ */ jsxs(Tabs, {
				defaultValue: "inventory",
				children: [
					/* @__PURE__ */ jsxs(TabsList, { children: [
						/* @__PURE__ */ jsx(TabsTrigger, {
							value: "inventory",
							children: "Inventory"
						}),
						/* @__PURE__ */ jsx(TabsTrigger, {
							value: "checkout",
							children: canManage ? "Checkout desk" : "My checkouts"
						}),
						/* @__PURE__ */ jsx(TabsTrigger, {
							value: "requests",
							children: canManage ? "Requests" : "My requests"
						})
					] }),
					/* @__PURE__ */ jsx(TabsContent, {
						value: "inventory",
						className: "space-y-3",
						children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, {
							className: "flex flex-row items-center justify-between flex-wrap gap-3",
							children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs(CardTitle, {
								className: "text-base flex items-center gap-2",
								children: [/* @__PURE__ */ jsx(Search, { className: "h-4 w-4 text-primary" }), " Inventory"]
							}), /* @__PURE__ */ jsx("p", {
								className: "text-xs text-muted-foreground",
								children: "Search across asset tag, name, brand, category and storage location"
							})] }), canManage && /* @__PURE__ */ jsxs(Dialog, {
								open,
								onOpenChange: setOpen,
								children: [/* @__PURE__ */ jsx(DialogTrigger, {
									asChild: true,
									children: /* @__PURE__ */ jsxs(Button, {
										size: "sm",
										className: "gap-1.5",
										children: [/* @__PURE__ */ jsx(Plus, { className: "h-3.5 w-3.5" }), " Add item"]
									})
								}), /* @__PURE__ */ jsxs(DialogContent, {
									className: "sm:max-w-lg",
									children: [
										/* @__PURE__ */ jsx(DialogHeader, { children: /* @__PURE__ */ jsx(DialogTitle, { children: "Add an inventory item" }) }),
										/* @__PURE__ */ jsxs("div", {
											className: "grid grid-cols-2 gap-3 py-2",
											children: [
												/* @__PURE__ */ jsx(Field, {
													label: "Asset tag",
													children: /* @__PURE__ */ jsx(Input, {
														value: form.assetTag,
														onChange: (e) => setForm({
															...form,
															assetTag: e.target.value.toUpperCase()
														}),
														placeholder: "PRJ-024"
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Name",
													children: /* @__PURE__ */ jsx(Input, {
														value: form.name,
														onChange: (e) => setForm({
															...form,
															name: e.target.value
														})
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Brand",
													children: /* @__PURE__ */ jsx(Input, {
														value: form.brand,
														onChange: (e) => setForm({
															...form,
															brand: e.target.value
														})
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Category",
													children: /* @__PURE__ */ jsxs(Select, {
														value: form.category,
														onValueChange: (v) => setForm({
															...form,
															category: v
														}),
														children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsx(SelectContent, { children: CATEGORIES.map((c) => /* @__PURE__ */ jsx(SelectItem, {
															value: c,
															children: c
														}, c)) })]
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Condition",
													children: /* @__PURE__ */ jsxs(Select, {
														value: form.condition,
														onValueChange: (v) => setForm({
															...form,
															condition: v
														}),
														children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsx(SelectContent, { children: CONDITIONS.map((c) => /* @__PURE__ */ jsx(SelectItem, {
															value: c,
															children: c
														}, c)) })]
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Quantity",
													children: /* @__PURE__ */ jsx(Input, {
														type: "number",
														min: 1,
														value: form.quantity,
														onChange: (e) => setForm({
															...form,
															quantity: Math.max(1, Number(e.target.value) || 1)
														})
													})
												}),
												/* @__PURE__ */ jsx(Field, {
													label: "Location",
													children: /* @__PURE__ */ jsx(Input, {
														value: form.location,
														onChange: (e) => setForm({
															...form,
															location: e.target.value
														}),
														placeholder: "AV Cage · A"
													})
												})
											]
										}),
										/* @__PURE__ */ jsx(DialogFooter, { children: /* @__PURE__ */ jsx(Button, {
											onClick: addItem,
											disabled: saving,
											children: saving ? "Adding…" : "Add to inventory"
										}) })
									]
								})]
							})]
						}), /* @__PURE__ */ jsxs(CardContent, {
							className: "space-y-3",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "grid grid-cols-1 md:grid-cols-[1fr_180px_180px] gap-2",
								children: [
									/* @__PURE__ */ jsxs("div", {
										className: "relative",
										children: [/* @__PURE__ */ jsx(Search, { className: "absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" }), /* @__PURE__ */ jsx(Input, {
											value: q,
											onChange: (e) => setQ(e.target.value),
											placeholder: "Search asset tag, name, brand…",
											className: "pl-9"
										})]
									}),
									/* @__PURE__ */ jsxs(Select, {
										value: cat,
										onValueChange: (v) => setCat(v),
										children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
											value: "all",
											children: "All categories"
										}), CATEGORIES.map((c) => /* @__PURE__ */ jsx(SelectItem, {
											value: c,
											children: c
										}, c))] })]
									}),
									/* @__PURE__ */ jsxs(Select, {
										value: status,
										onValueChange: (v) => setStatus(v),
										children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsxs(SelectContent, { children: [
											/* @__PURE__ */ jsx(SelectItem, {
												value: "all",
												children: "All statuses"
											}),
											/* @__PURE__ */ jsx(SelectItem, {
												value: "available",
												children: "Available"
											}),
											/* @__PURE__ */ jsx(SelectItem, {
												value: "checked_out",
												children: "Checked out"
											}),
											/* @__PURE__ */ jsx(SelectItem, {
												value: "maintenance",
												children: "Maintenance"
											})
										] })]
									})
								]
							}), /* @__PURE__ */ jsx("div", {
								className: "overflow-x-auto",
								children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
									/* @__PURE__ */ jsx(TableHead, { children: "Tag" }),
									/* @__PURE__ */ jsx(TableHead, { children: "Item" }),
									/* @__PURE__ */ jsx(TableHead, { children: "Category" }),
									/* @__PURE__ */ jsx(TableHead, {
										className: "text-right",
										children: "Qty"
									}),
									/* @__PURE__ */ jsx(TableHead, { children: "Condition" }),
									/* @__PURE__ */ jsx(TableHead, { children: "Location" }),
									/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
									/* @__PURE__ */ jsx(TableHead, {
										className: "text-right",
										children: "Actions"
									})
								] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [
									loadingItems && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
										colSpan: 8,
										className: "text-center text-muted-foreground py-6",
										children: "Loading…"
									}) }),
									!loadingItems && filtered.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
										colSpan: 8,
										className: "text-center text-muted-foreground py-6",
										children: "No items match."
									}) }),
									filtered.map((it) => {
										const Icon = CAT_ICON[it.category] ?? Laptop;
										const st = displayStatus(it);
										return /* @__PURE__ */ jsxs(TableRow, { children: [
											/* @__PURE__ */ jsx(TableCell, {
												className: "font-mono text-[11px]",
												children: it.assetTag ?? "—"
											}),
											/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsxs("div", {
												className: "flex items-center gap-2",
												children: [
													/* @__PURE__ */ jsx(Icon, { className: "h-3.5 w-3.5 text-muted-foreground" }),
													/* @__PURE__ */ jsx("span", {
														className: "font-medium",
														children: it.name
													}),
													/* @__PURE__ */ jsxs("span", {
														className: "text-[10px] text-muted-foreground",
														children: ["· ", it.brand]
													})
												]
											}) }),
											/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
												variant: "outline",
												className: "text-[10px]",
												children: it.category
											}) }),
											/* @__PURE__ */ jsx(TableCell, {
												className: "text-right font-mono",
												children: it.quantity
											}),
											/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
												variant: "outline",
												className: it.condition === "Excellent" ? "border-success/40 text-success" : it.condition === "Good" ? "border-primary/40 text-primary" : it.condition === "Worn" ? "border-warning/40 text-warning" : "border-destructive/40 text-destructive",
												children: it.condition
											}) }),
											/* @__PURE__ */ jsx(TableCell, {
												className: "text-xs text-muted-foreground",
												children: it.location
											}),
											/* @__PURE__ */ jsx(TableCell, { children: canManage && st !== "checked_out" ? /* @__PURE__ */ jsxs(Select, {
												value: st === "maintenance" ? "maintenance" : "available",
												onValueChange: (v) => setItemStatus(it, v),
												children: [/* @__PURE__ */ jsx(SelectTrigger, {
													className: "h-7 w-[140px] text-xs",
													children: /* @__PURE__ */ jsx(SelectValue, {})
												}), /* @__PURE__ */ jsxs(SelectContent, { children: [/* @__PURE__ */ jsx(SelectItem, {
													value: "available",
													children: "Available"
												}), /* @__PURE__ */ jsx(SelectItem, {
													value: "maintenance",
													children: "Maintenance"
												})] })]
											}) : /* @__PURE__ */ jsx(Badge, {
												variant: "outline",
												className: st === "available" ? "border-success/40 text-success" : st === "checked_out" ? "border-primary/40 text-primary" : "border-warning/40 text-warning",
												children: st.replace("_", " ")
											}) }),
											/* @__PURE__ */ jsx(TableCell, {
												className: "text-right",
												children: /* @__PURE__ */ jsxs("div", {
													className: "inline-flex gap-1",
													children: [!canManage && /* @__PURE__ */ jsx(Button, {
														size: "sm",
														variant: "outline",
														disabled: st !== "available" || requestingId === it._id,
														onClick: () => requestItem(it),
														children: requestingId === it._id ? "…" : "Request"
													}), canDelete && /* @__PURE__ */ jsx(Button, {
														size: "icon",
														variant: "ghost",
														className: "h-7 w-7 text-destructive",
														onClick: async () => {
															try {
																await deleteResource(it._id);
																toast.success("Item removed");
																refresh();
															} catch (e) {
																toast.error(e instanceof ApiError ? e.message : "Failed to remove item");
															}
														},
														children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
													})]
												})
											})
										] }, it._id);
									})
								] })] })
							})]
						})] })
					}),
					/* @__PURE__ */ jsx(TabsContent, {
						value: "checkout",
						children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsxs(CardTitle, {
							className: "text-base flex items-center gap-2",
							children: [
								/* @__PURE__ */ jsx(PackageCheck, { className: "h-4 w-4 text-primary" }),
								" ",
								canManage ? "Active equipment checkouts" : "My equipment checkouts"
							]
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "All borrowed IT assets flow through this desk."
						})] }), /* @__PURE__ */ jsx(CardContent, {
							className: "overflow-x-auto",
							children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
								/* @__PURE__ */ jsx(TableHead, { children: "Ticket" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Item" }),
								canManage && /* @__PURE__ */ jsx(TableHead, { children: "Borrower" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Due" }),
								/* @__PURE__ */ jsx(TableHead, {
									className: "text-right",
									children: "Action"
								})
							] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [
								loadingBookings && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
									colSpan: canManage ? 5 : 4,
									className: "text-center text-muted-foreground py-6",
									children: "Loading…"
								}) }),
								!loadingBookings && checkouts.map((c) => {
									const overdue = c.dueDate && new Date(c.dueDate) < /* @__PURE__ */ new Date();
									return /* @__PURE__ */ jsxs(TableRow, { children: [
										/* @__PURE__ */ jsx(TableCell, {
											className: "font-mono text-xs",
											children: c._id.slice(-6)
										}),
										/* @__PURE__ */ jsxs(TableCell, { children: [
											c.resource?.assetTag,
											" · ",
											c.resource?.name
										] }),
										canManage && /* @__PURE__ */ jsxs(TableCell, { children: [/* @__PURE__ */ jsx("p", {
											className: "text-sm",
											children: c.user?.name
										}), /* @__PURE__ */ jsx("p", {
											className: "text-[10px] text-muted-foreground font-mono",
											children: c.user?.matricule ?? c.user?.email
										})] }),
										/* @__PURE__ */ jsx(TableCell, { children: c.dueDate ? /* @__PURE__ */ jsxs(Badge, {
											variant: overdue ? "destructive" : "outline",
											className: !overdue ? "border-success/40 text-success" : "",
											children: [String(c.dueDate).slice(0, 10), overdue && " · overdue"]
										}) : /* @__PURE__ */ jsx("span", {
											className: "text-muted-foreground text-xs",
											children: "—"
										}) }),
										/* @__PURE__ */ jsx(TableCell, {
											className: "text-right",
											children: canManage && /* @__PURE__ */ jsx(Button, {
												size: "sm",
												variant: "outline",
												disabled: returningId === c._id,
												onClick: () => checkin(c),
												children: returningId === c._id ? "…" : "Check in"
											})
										})
									] }, c._id);
								}),
								!loadingBookings && checkouts.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
									colSpan: canManage ? 5 : 4,
									className: "text-center text-muted-foreground py-6",
									children: "No active checkouts."
								}) })
							] })] })
						})] })
					}),
					/* @__PURE__ */ jsx(TabsContent, {
						value: "requests",
						children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
							className: "text-base",
							children: canManage ? "Equipment requests" : "My requests"
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: canManage ? "Approve or reject borrower requests; approval issues a checkout" : "Track the status of your submitted requests"
						})] }), /* @__PURE__ */ jsx(CardContent, {
							className: "overflow-x-auto",
							children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
								/* @__PURE__ */ jsx(TableHead, { children: "Req" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Item" }),
								canManage && /* @__PURE__ */ jsx(TableHead, { children: "Requester" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Need by" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Purpose" }),
								/* @__PURE__ */ jsx(TableHead, { children: "Status" }),
								/* @__PURE__ */ jsx(TableHead, {
									className: "text-right",
									children: "Action"
								})
							] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [
								loadingBookings && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
									colSpan: canManage ? 7 : 6,
									className: "text-center text-muted-foreground py-6",
									children: "Loading…"
								}) }),
								!loadingBookings && requests.map((r) => /* @__PURE__ */ jsxs(TableRow, { children: [
									/* @__PURE__ */ jsx(TableCell, {
										className: "font-mono text-xs",
										children: r._id.slice(-6)
									}),
									/* @__PURE__ */ jsxs(TableCell, { children: [
										r.resource?.assetTag,
										" · ",
										r.resource?.name
									] }),
									canManage && /* @__PURE__ */ jsxs(TableCell, { children: [/* @__PURE__ */ jsx("p", {
										className: "text-sm",
										children: r.user?.name
									}), /* @__PURE__ */ jsx("p", {
										className: "text-[10px] text-muted-foreground font-mono",
										children: r.user?.matricule ?? r.user?.email
									})] }),
									/* @__PURE__ */ jsx(TableCell, {
										className: "font-mono text-xs",
										children: r.dueDate ? String(r.dueDate).slice(0, 10) : "—"
									}),
									/* @__PURE__ */ jsx(TableCell, {
										className: "text-xs max-w-[260px] truncate",
										title: r.purpose,
										children: r.purpose
									}),
									/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Badge, {
										variant: "outline",
										className: r.status === "approved" ? "border-success/40 text-success" : r.status === "rejected" ? "border-destructive/40 text-destructive" : "border-warning/40 text-warning",
										children: r.status
									}) }),
									/* @__PURE__ */ jsx(TableCell, {
										className: "text-right",
										children: canManage && r.status === "pending" && /* @__PURE__ */ jsxs("div", {
											className: "inline-flex gap-1",
											children: [/* @__PURE__ */ jsx(Button, {
												size: "sm",
												disabled: decidingId === r._id,
												onClick: () => decide(r, "approved"),
												children: "Approve"
											}), /* @__PURE__ */ jsx(Button, {
												size: "sm",
												variant: "outline",
												disabled: decidingId === r._id,
												onClick: () => decide(r, "rejected"),
												children: "Reject"
											})]
										})
									})
								] }, r._id)),
								!loadingBookings && requests.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
									colSpan: canManage ? 7 : 6,
									className: "text-center text-muted-foreground py-6",
									children: "No requests."
								}) })
							] })] })
						})] })
					})
				]
			})
		]
	})] });
}
function Metric({ icon: Icon, label, value, tone }) {
	return /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
		className: "p-4 flex items-center gap-3",
		children: [/* @__PURE__ */ jsx("div", {
			className: "h-10 w-10 rounded-lg bg-primary/15 ring-1 ring-primary/30 flex items-center justify-center",
			children: /* @__PURE__ */ jsx(Icon, { className: "h-4.5 w-4.5 text-primary" })
		}), /* @__PURE__ */ jsxs("div", {
			className: "min-w-0",
			children: [/* @__PURE__ */ jsx("p", {
				className: "text-[11px] uppercase tracking-wider text-muted-foreground truncate",
				children: label
			}), /* @__PURE__ */ jsx("p", {
				className: `text-lg font-semibold leading-tight ${tone === "warning" ? "text-warning" : tone === "success" ? "text-success" : tone === "primary" ? "text-primary" : ""}`,
				children: value
			})]
		})]
	}) });
}
function Field({ label, children }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "grid gap-1.5",
		children: [/* @__PURE__ */ jsx(Label, { children: label }), children]
	});
}
//#endregion
export { ITPage as component };
