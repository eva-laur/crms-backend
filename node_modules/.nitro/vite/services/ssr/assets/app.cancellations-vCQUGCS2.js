import { l as ApiError, s as useRole } from "./role-context-YJO8G-xL.js";
import { a as AppHeader, i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-eBpIeksT.js";
import { t as Button } from "./button-PJVP9td7.js";
import { t as Input } from "./input-B8Q2ztVi.js";
import { t as Label } from "./label-DBD1bRRP.js";
import { t as Textarea } from "./textarea-kko37XEX.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-Dg1urBTx.js";
import { c as getCourses } from "./academic-ThiXMN6N.js";
import { n as createCancellation, o as getCancellations } from "./coursework-HLqxs39D.js";
import { useMemo, useState } from "react";
import { Navigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { AlertTriangle, CalendarX } from "lucide-react";
//#region src/lib/academic-data.ts
var TIME_SLOTS = [
	"08:00–10:00",
	"10:00–12:00",
	"12:00–14:00",
	"14:00–16:00",
	"16:00–18:00"
];
//#endregion
//#region src/routes/app.cancellations.tsx?tsr-split=component
function CancellationsPage() {
	const { user, can } = useRole();
	if (!can("nav:cancellations") && user?.role !== "student") return /* @__PURE__ */ jsx(Navigate, { to: "/app/dashboard" });
	if (!user) return null;
	const canCancel = can("action:cancelClass");
	const queryClient = useQueryClient();
	const { data: cancellations = [], isLoading } = useQuery({
		queryKey: ["cancellations"],
		queryFn: getCancellations
	});
	const { data: courses = [] } = useQuery({
		queryKey: ["courses"],
		queryFn: getCourses
	});
	const myCourses = useMemo(() => user.role === "faculty" ? courses.filter((c) => c.lecturer?._id === user._id) : courses, [courses, user]);
	const [courseId, setCourseId] = useState("");
	const [date, setDate] = useState((/* @__PURE__ */ new Date()).toISOString().slice(0, 10));
	const [slot, setSlot] = useState(TIME_SLOTS[0]);
	const [reason, setReason] = useState("");
	const [submitting, setSubmitting] = useState(false);
	const activeCourseId = courseId || myCourses[0]?._id || "";
	const submit = async () => {
		if (!activeCourseId) return toast.error("No course available to cancel");
		if (!reason.trim()) return toast.error("Provide a reason");
		setSubmitting(true);
		try {
			const created = await createCancellation({
				course: activeCourseId,
				date,
				timeSlot: slot,
				reason: reason.trim()
			});
			queryClient.invalidateQueries({ queryKey: ["cancellations"] });
			toast.success("Class cancellation broadcast", { description: `${created.course?.courseCode ?? ""} · ${date} · ${slot} — students notified` });
			setReason("");
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Failed to cancel class");
		} finally {
			setSubmitting(false);
		}
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "Class Cancellations",
		subtitle: canCancel ? "Cancel a class on a date & time slot — concerned students are alerted" : "Cancellations affecting your enrolled courses"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-3 sm:px-5 lg:px-8 py-6 grid xl:grid-cols-[1fr_1.3fr] gap-5",
		children: [canCancel && /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsxs(CardTitle, {
			className: "text-base flex items-center gap-2",
			children: [/* @__PURE__ */ jsx(CalendarX, { className: "h-4 w-4 text-destructive" }), " Cancel a class"]
		}), /* @__PURE__ */ jsx("p", {
			className: "text-xs text-muted-foreground",
			children: "Notifies students enrolled in the course; admin is informed."
		})] }), /* @__PURE__ */ jsxs(CardContent, {
			className: "space-y-3",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "grid gap-1.5",
					children: [/* @__PURE__ */ jsx(Label, { children: "Course" }), /* @__PURE__ */ jsxs(Select, {
						value: activeCourseId,
						onValueChange: setCourseId,
						children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Select a course" }) }), /* @__PURE__ */ jsx(SelectContent, { children: myCourses.map((c) => /* @__PURE__ */ jsxs(SelectItem, {
							value: c._id,
							children: [
								c.courseCode,
								" · ",
								c.title
							]
						}, c._id)) })]
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "grid grid-cols-2 gap-3",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "grid gap-1.5",
						children: [/* @__PURE__ */ jsx(Label, { children: "Date" }), /* @__PURE__ */ jsx(Input, {
							type: "date",
							value: date,
							onChange: (e) => setDate(e.target.value)
						})]
					}), /* @__PURE__ */ jsxs("div", {
						className: "grid gap-1.5",
						children: [/* @__PURE__ */ jsx(Label, { children: "Time slot" }), /* @__PURE__ */ jsxs(Select, {
							value: slot,
							onValueChange: setSlot,
							children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, {}) }), /* @__PURE__ */ jsx(SelectContent, { children: TIME_SLOTS.map((s) => /* @__PURE__ */ jsx(SelectItem, {
								value: s,
								children: s
							}, s)) })]
						})]
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "grid gap-1.5",
					children: [/* @__PURE__ */ jsx(Label, { children: "Reason" }), /* @__PURE__ */ jsx(Textarea, {
						rows: 3,
						value: reason,
						onChange: (e) => setReason(e.target.value),
						placeholder: "Conference travel, illness, …"
					})]
				}),
				/* @__PURE__ */ jsx(Button, {
					variant: "destructive",
					onClick: submit,
					disabled: submitting,
					children: submitting ? "Cancelling…" : "Cancel class & notify"
				})
			]
		})] }), /* @__PURE__ */ jsxs(Card, {
			className: !canCancel ? "xl:col-span-2" : "",
			children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
				className: "text-base",
				children: "Cancellation log"
			}), /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground",
				children: isLoading ? "Loading…" : `${cancellations.length} record(s)`
			})] }), /* @__PURE__ */ jsx(CardContent, {
				className: "p-0",
				children: /* @__PURE__ */ jsxs("ul", {
					className: "divide-y divide-border",
					children: [!isLoading && cancellations.length === 0 && /* @__PURE__ */ jsx("li", {
						className: "px-5 py-6 text-center text-sm text-muted-foreground",
						children: "No cancellations."
					}), cancellations.map((c) => /* @__PURE__ */ jsxs("li", {
						className: "px-3 sm:px-5 py-4",
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "flex items-center gap-2 flex-wrap",
								children: [/* @__PURE__ */ jsx(AlertTriangle, { className: "h-3.5 w-3.5 text-destructive" }), /* @__PURE__ */ jsxs("p", {
									className: "text-sm font-semibold",
									children: [
										c.course?.courseCode ?? "—",
										" cancelled · ",
										c.date,
										" · ",
										c.timeSlot
									]
								})]
							}),
							/* @__PURE__ */ jsx("p", {
								className: "text-sm text-muted-foreground mt-1",
								children: c.reason
							}),
							/* @__PURE__ */ jsxs("p", {
								className: "text-[11px] text-muted-foreground mt-1.5",
								children: [
									"— ",
									c.cancelledBy?.name ?? "Unknown",
									" · posted ",
									new Date(c.createdAt).toLocaleString()
								]
							})
						]
					}, c._id))]
				})
			})]
		})]
	})] });
}
//#endregion
export { CancellationsPage as component };
