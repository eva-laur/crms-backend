import { l as ApiError, s as useRole } from "./role-context-YJO8G-xL.js";
import { a as AppHeader, i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-eBpIeksT.js";
import { t as Button } from "./button-PJVP9td7.js";
import { t as Input } from "./input-B8Q2ztVi.js";
import { t as Label } from "./label-DBD1bRRP.js";
import { t as Textarea } from "./textarea-kko37XEX.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-Dg1urBTx.js";
import { c as getCourses } from "./academic-ThiXMN6N.js";
import { c as getSubmissions, d as uploadMaterial, i as deleteSubmission, l as recordMaterialDownload, r as deleteMaterial, s as getMaterials, u as submitAssignment } from "./coursework-HLqxs39D.js";
import { i as TabsTrigger, n as TabsContent, r as TabsList, t as Tabs } from "./tabs-CCJRliUM.js";
import { a as TableHeader, i as TableHead, n as TableBody, o as TableRow, r as TableCell, t as Table } from "./table-C0WYWEQX.js";
import { useMemo, useState } from "react";
import { Navigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Download, FileText, Send, Trash2, UploadCloud } from "lucide-react";
//#region src/routes/app.course-materials.tsx?tsr-split=component
var MAX_FILE_BYTES = 20 * 1024 * 1024;
var humanSize = (bytes) => bytes < 1024 ? `${bytes} B` : bytes < 1024 * 1024 ? `${(bytes / 1024).toFixed(1)} KB` : `${(bytes / 1024 / 1024).toFixed(2)} MB`;
function MaterialsPage() {
	const { user } = useRole();
	if (!user) return /* @__PURE__ */ jsx(Navigate, { to: "/" });
	if (user.role !== "faculty" && user.role !== "student") return /* @__PURE__ */ jsx(Navigate, { to: "/app/dashboard" });
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "Course Materials",
		subtitle: "Upload lecture material and receive student submissions."
	}), /* @__PURE__ */ jsx("div", {
		className: "px-5 lg:px-8 py-6",
		children: user.role === "faculty" ? /* @__PURE__ */ jsx(FacultyView, {}) : /* @__PURE__ */ jsx(StudentView, {})
	})] });
}
function FacultyView() {
	const { user } = useRole();
	const queryClient = useQueryClient();
	const invalidate = () => {
		queryClient.invalidateQueries({ queryKey: ["materials"] });
		queryClient.invalidateQueries({ queryKey: ["submissions"] });
	};
	const { data: courses = [] } = useQuery({
		queryKey: ["courses"],
		queryFn: getCourses
	});
	const { data: materials = [] } = useQuery({
		queryKey: ["materials"],
		queryFn: getMaterials
	});
	const { data: submissions = [] } = useQuery({
		queryKey: ["submissions"],
		queryFn: getSubmissions
	});
	const myCourses = useMemo(() => courses.filter((c) => c.lecturer?._id === user._id), [courses, user]);
	const myMaterials = useMemo(() => materials.filter((m) => m.uploadedBy?._id === user._id), [materials, user]);
	const [courseId, setCourseId] = useState("");
	const [title, setTitle] = useState("");
	const [file, setFile] = useState(null);
	const [uploading, setUploading] = useState(false);
	const activeCourseId = courseId || myCourses[0]?._id || "";
	const upload = async () => {
		if (!activeCourseId || !title.trim() || !file) return toast.error("Course, title and file are required");
		if (file.size > MAX_FILE_BYTES) return toast.error("Max file size is 20 MB");
		setUploading(true);
		try {
			await uploadMaterial({
				course: activeCourseId,
				title: title.trim(),
				file
			});
			toast.success("Material uploaded");
			setTitle("");
			setFile(null);
			invalidate();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Upload failed");
		} finally {
			setUploading(false);
		}
	};
	const remove = async (id) => {
		try {
			await deleteMaterial(id);
			invalidate();
			toast.success("Deleted");
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Delete failed");
		}
	};
	const removeSubmission = async (id) => {
		try {
			await deleteSubmission(id);
			invalidate();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Delete failed");
		}
	};
	return /* @__PURE__ */ jsxs(Tabs, {
		defaultValue: "upload",
		className: "space-y-4",
		children: [
			/* @__PURE__ */ jsxs(TabsList, { children: [
				/* @__PURE__ */ jsx(TabsTrigger, {
					value: "upload",
					children: "Upload material"
				}),
				/* @__PURE__ */ jsxs(TabsTrigger, {
					value: "mine",
					children: [
						"My materials (",
						myMaterials.length,
						")"
					]
				}),
				/* @__PURE__ */ jsxs(TabsTrigger, {
					value: "incoming",
					children: [
						"Student submissions (",
						submissions.length,
						")"
					]
				})
			] }),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "upload",
				children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsxs(CardTitle, {
					className: "text-base flex items-center gap-2",
					children: [/* @__PURE__ */ jsx(UploadCloud, { className: "h-4 w-4 text-primary" }), " New material"]
				}) }), /* @__PURE__ */ jsxs(CardContent, {
					className: "grid md:grid-cols-2 gap-4",
					children: [
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Course" }), /* @__PURE__ */ jsxs(Select, {
							value: activeCourseId,
							onValueChange: setCourseId,
							children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Select course" }) }), /* @__PURE__ */ jsx(SelectContent, { children: myCourses.map((c) => /* @__PURE__ */ jsxs(SelectItem, {
								value: c._id,
								children: [
									c.courseCode,
									" — ",
									c.title
								]
							}, c._id)) })]
						})] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Title" }), /* @__PURE__ */ jsx(Input, {
							value: title,
							onChange: (e) => setTitle(e.target.value),
							placeholder: "Week 3 — TCP handshake"
						})] }),
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-2",
							children: [
								/* @__PURE__ */ jsx(Label, { children: "File (≤ 20 MB)" }),
								/* @__PURE__ */ jsx(Input, {
									type: "file",
									onChange: (e) => setFile(e.target.files?.[0] ?? null)
								}),
								file && /* @__PURE__ */ jsxs("p", {
									className: "text-[11px] text-muted-foreground mt-1",
									children: [
										file.name,
										" · ",
										humanSize(file.size)
									]
								})
							]
						}),
						/* @__PURE__ */ jsx("div", {
							className: "md:col-span-2",
							children: /* @__PURE__ */ jsxs(Button, {
								onClick: upload,
								disabled: uploading,
								children: [
									/* @__PURE__ */ jsx(UploadCloud, { className: "h-4 w-4 mr-1.5" }),
									" ",
									uploading ? "Uploading…" : "Upload"
								]
							})
						})
					]
				})] })
			}),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "mine",
				children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Course" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Title" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Size" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Downloads" }),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Actions"
						})
					] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [myMaterials.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 5,
						className: "text-center text-muted-foreground py-6",
						children: "No uploads yet."
					}) }), myMaterials.map((m) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-xs",
							children: m.course?.courseCode ?? "—"
						}),
						/* @__PURE__ */ jsx(TableCell, { children: m.title }),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-xs text-muted-foreground",
							children: humanSize(m.fileSize)
						}),
						/* @__PURE__ */ jsx(TableCell, { children: m.downloads.length }),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "text-right space-x-1",
							children: [/* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								asChild: true,
								children: /* @__PURE__ */ jsx("a", {
									href: m.fileUrl,
									target: "_blank",
									rel: "noreferrer",
									download: m.fileName,
									children: /* @__PURE__ */ jsx(Download, { className: "h-3.5 w-3.5" })
								})
							}), /* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								className: "text-destructive",
								onClick: () => remove(m._id),
								children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
							})]
						})
					] }, m._id))] })] })
				}) })
			}),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "incoming",
				children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "From" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Course" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Note" }),
						/* @__PURE__ */ jsx(TableHead, { children: "File" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Received" }),
						/* @__PURE__ */ jsx(TableHead, {})
					] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [submissions.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 6,
						className: "text-center text-muted-foreground py-6",
						children: "No submissions."
					}) }), submissions.map((s) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsxs(TableCell, { children: [/* @__PURE__ */ jsx("div", {
							className: "text-sm font-medium",
							children: s.student?.name
						}), /* @__PURE__ */ jsx("div", {
							className: "text-[10px] font-mono text-muted-foreground",
							children: s.student?.matricule
						})] }),
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-xs",
							children: s.course?.courseCode ?? "—"
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "max-w-xs text-xs",
							children: s.note || /* @__PURE__ */ jsx("span", {
								className: "text-muted-foreground",
								children: "—"
							})
						}),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "text-xs",
							children: [
								s.fileName,
								" ",
								/* @__PURE__ */ jsxs("span", {
									className: "text-muted-foreground",
									children: [
										"(",
										humanSize(s.fileSize),
										")"
									]
								})
							]
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-[11px] text-muted-foreground",
							children: new Date(s.createdAt).toLocaleString()
						}),
						/* @__PURE__ */ jsxs(TableCell, {
							className: "text-right space-x-1",
							children: [/* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								asChild: true,
								children: /* @__PURE__ */ jsx("a", {
									href: s.fileUrl,
									target: "_blank",
									rel: "noreferrer",
									download: s.fileName,
									children: /* @__PURE__ */ jsx(Download, { className: "h-3.5 w-3.5" })
								})
							}), /* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								className: "text-destructive",
								onClick: () => removeSubmission(s._id),
								children: /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" })
							})]
						})
					] }, s._id))] })] })
				}) })
			})
		]
	});
}
function StudentView() {
	const { user } = useRole();
	const queryClient = useQueryClient();
	const invalidate = () => {
		queryClient.invalidateQueries({ queryKey: ["materials"] });
		queryClient.invalidateQueries({ queryKey: ["submissions"] });
	};
	const { data: courses = [] } = useQuery({
		queryKey: ["courses"],
		queryFn: getCourses
	});
	const { data: materials = [] } = useQuery({
		queryKey: ["materials"],
		queryFn: getMaterials
	});
	const { data: mySubs = [] } = useQuery({
		queryKey: ["submissions"],
		queryFn: getSubmissions
	});
	const myCourses = useMemo(() => courses.filter((c) => c.students?.some((s) => s._id === user._id)), [courses, user]);
	const [courseId, setCourseId] = useState("");
	const [note, setNote] = useState("");
	const [file, setFile] = useState(null);
	const [sending, setSending] = useState(false);
	const activeCourseId = courseId || myCourses[0]?._id || "";
	const download = async (m) => {
		window.open(m.fileUrl, "_blank");
		try {
			await recordMaterialDownload(m._id);
			invalidate();
		} catch {}
	};
	const send = async () => {
		if (!activeCourseId || !file) return toast.error("Course and file required");
		if (file.size > MAX_FILE_BYTES) return toast.error("Max file size is 20 MB");
		setSending(true);
		try {
			await submitAssignment({
				course: activeCourseId,
				note,
				file
			});
			toast.success("Sent to lecturer");
			setNote("");
			setFile(null);
			invalidate();
		} catch (e) {
			toast.error(e instanceof ApiError ? e.message : "Send failed");
		} finally {
			setSending(false);
		}
	};
	return /* @__PURE__ */ jsxs(Tabs, {
		defaultValue: "download",
		className: "space-y-4",
		children: [
			/* @__PURE__ */ jsxs(TabsList, { children: [
				/* @__PURE__ */ jsxs(TabsTrigger, {
					value: "download",
					children: [
						"Course materials (",
						materials.length,
						")"
					]
				}),
				/* @__PURE__ */ jsx(TabsTrigger, {
					value: "submit",
					children: "Send to lecturer"
				}),
				/* @__PURE__ */ jsxs(TabsTrigger, {
					value: "mine",
					children: [
						"My submissions (",
						mySubs.length,
						")"
					]
				})
			] }),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "download",
				children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Course" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Title" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Lecturer" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Uploaded" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Size" }),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Action"
						})
					] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [materials.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 6,
						className: "text-center text-muted-foreground py-6",
						children: "No materials for your courses yet."
					}) }), materials.map((m) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-xs",
							children: m.course?.courseCode ?? "—"
						}),
						/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-2",
							children: [/* @__PURE__ */ jsx(FileText, { className: "h-3.5 w-3.5 text-primary" }), m.title]
						}) }),
						/* @__PURE__ */ jsx(TableCell, { children: m.uploadedBy?.name ?? "—" }),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-[11px] text-muted-foreground",
							children: new Date(m.createdAt).toLocaleDateString()
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-xs",
							children: humanSize(m.fileSize)
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-right",
							children: /* @__PURE__ */ jsxs(Button, {
								size: "sm",
								variant: "outline",
								onClick: () => download(m),
								children: [/* @__PURE__ */ jsx(Download, { className: "h-3.5 w-3.5 mr-1" }), "Download"]
							})
						})
					] }, m._id))] })] })
				}) })
			}),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "submit",
				children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsxs(CardTitle, {
					className: "text-base flex items-center gap-2",
					children: [/* @__PURE__ */ jsx(Send, { className: "h-4 w-4 text-primary" }), " Send document to a lecturer"]
				}) }), /* @__PURE__ */ jsxs(CardContent, {
					className: "grid md:grid-cols-2 gap-4",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-2",
							children: [
								/* @__PURE__ */ jsx(Label, { children: "Course (must be enrolled)" }),
								/* @__PURE__ */ jsxs(Select, {
									value: activeCourseId,
									onValueChange: setCourseId,
									children: [/* @__PURE__ */ jsx(SelectTrigger, { children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Select course" }) }), /* @__PURE__ */ jsx(SelectContent, { children: myCourses.map((c) => /* @__PURE__ */ jsxs(SelectItem, {
										value: c._id,
										children: [
											c.courseCode,
											" — ",
											c.title,
											" (",
											c.lecturer?.name,
											")"
										]
									}, c._id)) })]
								}),
								myCourses.length === 0 && /* @__PURE__ */ jsx("p", {
									className: "text-xs text-muted-foreground mt-1",
									children: "You're not enrolled in any course yet."
								})
							]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-2",
							children: [/* @__PURE__ */ jsx(Label, { children: "Note" }), /* @__PURE__ */ jsx(Textarea, {
								value: note,
								onChange: (e) => setNote(e.target.value),
								placeholder: "Optional message to the lecturer"
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "md:col-span-2",
							children: [
								/* @__PURE__ */ jsx(Label, { children: "File (≤ 20 MB)" }),
								/* @__PURE__ */ jsx(Input, {
									type: "file",
									onChange: (e) => setFile(e.target.files?.[0] ?? null)
								}),
								file && /* @__PURE__ */ jsxs("p", {
									className: "text-[11px] text-muted-foreground mt-1",
									children: [
										file.name,
										" · ",
										humanSize(file.size)
									]
								})
							]
						}),
						/* @__PURE__ */ jsx("div", {
							className: "md:col-span-2",
							children: /* @__PURE__ */ jsxs(Button, {
								onClick: send,
								disabled: sending,
								children: [
									/* @__PURE__ */ jsx(Send, { className: "h-4 w-4 mr-1.5" }),
									" ",
									sending ? "Sending…" : "Send"
								]
							})
						})
					]
				})] })
			}),
			/* @__PURE__ */ jsx(TabsContent, {
				value: "mine",
				children: /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Course" }),
						/* @__PURE__ */ jsx(TableHead, { children: "File" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Sent" }),
						/* @__PURE__ */ jsx(TableHead, {})
					] }) }), /* @__PURE__ */ jsxs(TableBody, { children: [mySubs.length === 0 && /* @__PURE__ */ jsx(TableRow, { children: /* @__PURE__ */ jsx(TableCell, {
						colSpan: 4,
						className: "text-center text-muted-foreground py-6",
						children: "No submissions yet."
					}) }), mySubs.map((s) => /* @__PURE__ */ jsxs(TableRow, { children: [
						/* @__PURE__ */ jsx(TableCell, {
							className: "font-mono text-xs",
							children: s.course?.courseCode ?? "—"
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-xs",
							children: s.fileName
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-[11px] text-muted-foreground",
							children: new Date(s.createdAt).toLocaleString()
						}),
						/* @__PURE__ */ jsx(TableCell, {
							className: "text-right",
							children: /* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								asChild: true,
								children: /* @__PURE__ */ jsx("a", {
									href: s.fileUrl,
									target: "_blank",
									rel: "noreferrer",
									download: s.fileName,
									children: /* @__PURE__ */ jsx(Download, { className: "h-3.5 w-3.5" })
								})
							})
						})
					] }, s._id))] })] })
				}) })
			})
		]
	});
}
//#endregion
export { MaterialsPage as component };
