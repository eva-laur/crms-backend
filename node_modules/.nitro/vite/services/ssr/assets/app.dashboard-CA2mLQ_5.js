import { d as apiRequest, o as isManager, r as ROLE_META, s as useRole } from "./role-context-YJO8G-xL.js";
import { a as AppHeader, i as CardTitle, n as CardContent, o as Badge, r as CardHeader, t as Card } from "./card-eBpIeksT.js";
import { c as getCourses, l as getResults, o as getAttendance } from "./academic-ThiXMN6N.js";
import { i as getOverdueBookings, r as getBookings } from "./bookings-BMnMGXf-.js";
import { i as getRoutes, n as getFleet, t as getAllReservations } from "./bus-CesgH2y4.js";
import { n as getAllUsers } from "./users-BpEolhg7.js";
import { i as getResources } from "./resources-CdRZ6Kye.js";
import { t as Progress } from "./progress-DOIEKRJF.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Activity, AlertTriangle, BookOpen, Boxes, Bus, CalendarClock, CheckCircle2, FlaskConical, GraduationCap, Laptop, Library, PackageCheck, Users, Wrench } from "lucide-react";
import { Bar, CartesianGrid, Cell, ComposedChart, Legend, Line, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/lib/api/reports.ts
var getResourceTypeUtilization = () => apiRequest("/reports/utilization/by-type");
var getAuditLogs = (query) => apiRequest("/audit", { query });
//#endregion
//#region src/routes/app.dashboard.tsx?tsr-split=component
function Dashboard() {
	const { user } = useRole();
	if (!user) return null;
	if (user.role === "admin") return /* @__PURE__ */ jsx(OperationsDashboard, {});
	if (user.role === "lab_manager") return /* @__PURE__ */ jsx(OperationsDashboard, {});
	if (isManager(user.role)) return /* @__PURE__ */ jsx(ScopedManagerDashboard, {});
	if (user.role === "faculty") return /* @__PURE__ */ jsx(FacultyDashboard, {});
	return /* @__PURE__ */ jsx(StudentDashboard, {});
}
var PALETTE = [
	"oklch(0.62 0.16 155)",
	"oklch(0.82 0.17 85)",
	"oklch(0.55 0.12 170)",
	"oklch(0.7 0.14 110)",
	"oklch(0.6 0.18 30)",
	"oklch(0.65 0.15 260)"
];
function isToday(iso) {
	if (!iso) return false;
	const d = new Date(iso);
	const now = /* @__PURE__ */ new Date();
	return d.getFullYear() === now.getFullYear() && d.getMonth() === now.getMonth() && d.getDate() === now.getDate();
}
function fmtWindow(b) {
	if (!b.startTime) return b.dueDate ? `Due ${new Date(b.dueDate).toLocaleDateString()}` : "—";
	const start = new Date(b.startTime);
	const end = b.endTime ? new Date(b.endTime) : null;
	const day = start.toLocaleDateString(void 0, { weekday: "short" });
	const startT = start.toLocaleTimeString(void 0, {
		hour: "2-digit",
		minute: "2-digit"
	});
	const endT = end ? end.toLocaleTimeString(void 0, {
		hour: "2-digit",
		minute: "2-digit"
	}) : "";
	return `${day} · ${startT}${endT ? `–${endT}` : ""}`;
}
function ScopedManagerDashboard() {
	const { user } = useRole();
	const { data: bookings = [], isLoading: loadingBookings } = useQuery({
		queryKey: ["bookings", "mine"],
		queryFn: () => getBookings()
	});
	const { data: overdue = [] } = useQuery({
		queryKey: ["bookings", "overdue"],
		queryFn: () => getOverdueBookings()
	});
	const isLibrary = user?.role === "library_manager";
	const isIT = user?.role === "it_manager";
	const isLogistics = user?.role === "logistics_manager";
	const { data: resources = [], isLoading: loadingResources } = useQuery({
		queryKey: ["resources", isLibrary ? "book" : "equipment"],
		queryFn: () => getResources({ type: isLibrary ? "book" : "equipment" }),
		enabled: !isLogistics
	});
	const { data: fleet = [] } = useQuery({
		queryKey: ["bus", "fleet"],
		queryFn: getFleet,
		enabled: isLogistics
	});
	const { data: reservations = [] } = useQuery({
		queryKey: ["bus", "reservations"],
		queryFn: getAllReservations,
		enabled: isLogistics
	});
	const { data: routes = [] } = useQuery({
		queryKey: ["bus", "routes"],
		queryFn: getRoutes,
		enabled: isLogistics
	});
	if (!user) return null;
	let title = "", subtitle = "", workspaceTo = "/app/library";
	let workspaceLabel = "";
	let Icon = Library;
	let metrics = [];
	if (isLibrary) {
		Icon = Library;
		title = "Library Catalog Console";
		subtitle = "Books, academic reports & novels — circulation & inventory";
		workspaceTo = "/app/library";
		workspaceLabel = "Open Library Catalog";
		const totalCopies = resources.reduce((s, r) => s + Number(r.totalCopies ?? r.quantity ?? 0), 0);
		const availableCopies = resources.reduce((s, r) => s + Number(r.availableCopies ?? 0), 0);
		const activeLoans = bookings.filter((b) => b.resource?.type === "book" && ["approved", "checked_out"].includes(b.status)).length;
		const overdueBooks = overdue.filter((b) => b.resource?.type === "book").length;
		metrics = [
			{
				label: "Titles in catalog",
				value: String(resources.length)
			},
			{
				label: "Copies available",
				value: String(availableCopies || totalCopies),
				tone: "success"
			},
			{
				label: "Active loans",
				value: String(activeLoans),
				tone: "primary"
			},
			{
				label: "Overdue returns",
				value: String(overdueBooks),
				tone: overdueBooks > 0 ? "warning" : void 0
			}
		];
	} else if (isIT) {
		Icon = Laptop;
		title = "IT Equipment Console";
		subtitle = "Projectors · cameras · laptops · microphones · displays";
		workspaceTo = "/app/it-equipment";
		workspaceLabel = "Open IT Equipment";
		const available = resources.filter((r) => r.status === "available").length;
		const checkedOut = bookings.filter((b) => b.resource?.type === "equipment" && b.status === "checked_out").length;
		const pending = bookings.filter((b) => b.resource?.type === "equipment" && b.status === "pending").length;
		metrics = [
			{
				label: "Items tracked",
				value: String(resources.length)
			},
			{
				label: "Available",
				value: String(available),
				tone: "success"
			},
			{
				label: "Checked out",
				value: String(checkedOut),
				tone: "primary"
			},
			{
				label: "Pending requests",
				value: String(pending),
				tone: pending > 0 ? "warning" : void 0
			}
		];
	} else if (isLogistics) {
		Icon = Bus;
		title = "Fleet & Logistics Console";
		subtitle = "Fleet · routes · reservations · fuel & mileage · maintenance";
		workspaceTo = "/app/bus";
		workspaceLabel = "Open Bus & Transport";
		const active = fleet.filter((b) => b.status === "active").length;
		const maintenanceOpen = fleet.filter((b) => b.status === "under_maintenance").length;
		const confirmedSeats = reservations.filter((r) => r.status === "confirmed").length;
		const activeRoutes = routes.filter((r) => r.isActive).length;
		metrics = [
			{
				label: "Buses active",
				value: `${active} / ${fleet.length}`,
				tone: "success"
			},
			{
				label: "Active routes",
				value: String(activeRoutes)
			},
			{
				label: "Seats confirmed",
				value: String(confirmedSeats),
				tone: "primary"
			},
			{
				label: "Maintenance open",
				value: String(maintenanceOpen),
				tone: maintenanceOpen > 0 ? "warning" : void 0
			}
		];
	}
	const loading = loadingBookings || !isLogistics && loadingResources;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title,
		subtitle
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-5 lg:px-8 py-6 space-y-6",
		children: [
			/* @__PURE__ */ jsx(Card, {
				className: "bg-card/60",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "p-5 flex items-center justify-between gap-4 flex-wrap",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-3",
						children: [/* @__PURE__ */ jsx("div", {
							className: "h-11 w-11 rounded-xl bg-primary/15 ring-1 ring-primary/40 flex items-center justify-center",
							children: /* @__PURE__ */ jsx(Icon, { className: "h-5 w-5 text-primary" })
						}), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm font-medium",
							children: ROLE_META[user.role].label
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[11px] text-muted-foreground",
							children: ROLE_META[user.role].tagline
						})] })]
					}), /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2",
						children: [/* @__PURE__ */ jsx(Badge, {
							variant: "outline",
							className: "border-primary/40 text-primary",
							children: "SCOPED VIEW"
						}), /* @__PURE__ */ jsx("a", {
							href: workspaceTo,
							className: "text-xs font-medium px-3 py-1.5 rounded-md bg-primary text-primary-foreground hover:opacity-90",
							children: workspaceLabel
						})]
					})]
				})
			}),
			/* @__PURE__ */ jsx("div", {
				className: "grid grid-cols-2 lg:grid-cols-4 gap-4",
				children: loading ? Array.from({ length: 4 }).map((_, i) => /* @__PURE__ */ jsx(Card, {
					className: "bg-card/60",
					children: /* @__PURE__ */ jsx(CardContent, {
						className: "p-5",
						children: /* @__PURE__ */ jsx("p", {
							className: "text-2xl font-semibold text-muted-foreground",
							children: "…"
						})
					})
				}, i)) : metrics.map((m) => /* @__PURE__ */ jsx(Card, {
					className: `bg-card/60 ${m.tone === "warning" ? "ring-1 ring-warning/30" : ""}`,
					children: /* @__PURE__ */ jsxs(CardContent, {
						className: "p-5",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-[11px] uppercase tracking-wider text-muted-foreground",
							children: m.label
						}), /* @__PURE__ */ jsx("p", {
							className: `mt-3 text-2xl font-semibold tracking-tight ${m.tone === "warning" ? "text-warning" : m.tone === "success" ? "text-success" : m.tone === "primary" ? "text-primary" : ""}`,
							children: m.value
						})]
					})
				}, m.label))
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "bg-card/60",
				children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Your workspace"
				}), /* @__PURE__ */ jsxs("p", {
					className: "text-xs text-muted-foreground",
					children: [
						"You only see ",
						ROLE_META[user.role].short.toLowerCase(),
						" resources — other domains are managed by their respective managers."
					]
				})] }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs("ul", {
					className: "grid sm:grid-cols-2 gap-2 text-sm",
					children: [/* @__PURE__ */ jsxs("li", {
						className: "rounded-lg border border-border p-3",
						children: [/* @__PURE__ */ jsxs("a", {
							href: workspaceTo,
							className: "font-medium text-primary hover:underline",
							children: ["→ ", workspaceLabel]
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[11px] text-muted-foreground mt-0.5",
							children: "Manage inventory, requests and circulation."
						})]
					}), /* @__PURE__ */ jsxs("li", {
						className: "rounded-lg border border-border p-3",
						children: [/* @__PURE__ */ jsx("a", {
							href: "/app/reports",
							className: "font-medium text-primary hover:underline",
							children: "→ Departmental Reports & Analytics"
						}), /* @__PURE__ */ jsx("p", {
							className: "text-[11px] text-muted-foreground mt-0.5",
							children: "Weekly · monthly · yearly · with PDF/Excel/CSV export."
						})]
					})]
				}) })]
			})
		]
	})] });
}
function OperationsDashboard() {
	const { user, can } = useRole();
	const showUsers = can("nav:users");
	const showAudit = can("nav:audit");
	const { data: bookings = [], isLoading: loadingBookings } = useQuery({
		queryKey: ["bookings", "mine"],
		queryFn: () => getBookings()
	});
	const { data: resources = [], isLoading: loadingResources } = useQuery({
		queryKey: ["resources", "all"],
		queryFn: () => getResources()
	});
	const { data: users = [] } = useQuery({
		queryKey: ["users", "all"],
		queryFn: getAllUsers,
		enabled: showUsers
	});
	const { data: typeUtilization = [] } = useQuery({
		queryKey: [
			"reports",
			"utilization",
			"by-type"
		],
		queryFn: getResourceTypeUtilization
	});
	const { data: auditData } = useQuery({
		queryKey: ["audit", "recent"],
		queryFn: () => getAuditLogs({ limit: 8 }),
		enabled: showAudit
	});
	if (!user) return null;
	const isLabManager = user.role === "lab_manager";
	const labResources = resources.filter((r) => r.type === "lab");
	const now = Date.now();
	const labsOccupiedNow = bookings.filter((b) => b.resource?.type === "lab" && b.startTime && b.endTime && new Date(b.startTime).getTime() <= now && new Date(b.endTime).getTime() >= now && ["approved", "checked_out"].includes(b.status)).length;
	const labOccupancyPct = labResources.length ? Math.round(labsOccupiedNow / labResources.length * 100) : 0;
	const headerTitle = isLabManager ? "Lab Halls Console" : showAudit ? "Operations Console" : "Resource Operations";
	const bookingsToday = bookings.filter((b) => isToday(b.startTime)).length;
	const activeCheckouts = bookings.filter((b) => b.status === "checked_out").length;
	const pendingRequests = bookings.filter((b) => b.status === "pending").length;
	const maintenanceItems = resources.filter((r) => r.status === "under_maintenance");
	const utilizationData = Array.from({ length: 12 }, (_, i) => {
		const hour = 8 + i;
		const count = bookings.filter((b) => {
			if (!b.startTime) return false;
			const d = new Date(b.startTime);
			return isToday(b.startTime) && d.getHours() === hour;
		}).length;
		return {
			hour: `${hour}:00`,
			bookings: count,
			occupancy: resources.length ? Math.round(count / resources.length * 100) : 0
		};
	});
	const categoryData = typeUtilization.map((t, i) => ({
		name: t.resourceType ? String(t.resourceType).replace(/^\w/, (c) => c.toUpperCase()) : "Other",
		value: t.bookingCount ?? 0,
		color: PALETTE[i % PALETTE.length]
	}));
	const auditRows = (auditData?.logs ?? []).map((a) => ({
		t: new Date(a.createdAt).toLocaleTimeString(void 0, {
			hour: "2-digit",
			minute: "2-digit",
			second: "2-digit"
		}),
		a: (a.action || "").split("_")[0] || "EVENT",
		who: a.actor?.name ?? a.actorRole ?? "system",
		msg: `${a.action ?? ""} · ${a.module ?? ""}${a.status && a.status >= 400 ? " (failed)" : ""}`
	}));
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: headerTitle,
		subtitle: showAudit ? "Live overview of campus resources, conflicts and infrastructure" : isLabManager ? `${ROLE_META[user.role].label} workspace — ${ROLE_META[user.role].tagline}` : "Resource utilization, checkouts and conflict queue"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-5 lg:px-8 py-6 space-y-6",
		children: [
			isLabManager && /* @__PURE__ */ jsx(Card, {
				className: "bg-card/60",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "p-5 flex items-center gap-4",
					children: [
						/* @__PURE__ */ jsx("div", {
							className: "h-11 w-11 rounded-xl bg-primary/15 ring-1 ring-primary/40 flex items-center justify-center",
							children: /* @__PURE__ */ jsx(FlaskConical, { className: "h-5 w-5 text-primary" })
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "grid grid-cols-2 gap-8 flex-1",
							children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
								className: "text-[11px] uppercase tracking-wider text-muted-foreground",
								children: "Lab rooms supervised"
							}), /* @__PURE__ */ jsx("p", {
								className: "text-2xl font-semibold tracking-tight",
								children: loadingResources ? "…" : labResources.length
							})] }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
								className: "text-[11px] uppercase tracking-wider text-muted-foreground",
								children: "Occupancy now"
							}), /* @__PURE__ */ jsx("p", {
								className: "text-2xl font-semibold tracking-tight",
								children: loadingBookings ? "…" : `${labOccupancyPct}%`
							})] })]
						}),
						/* @__PURE__ */ jsx(Badge, {
							variant: "outline",
							className: "border-primary/40 text-primary",
							children: "SPECIALIZED"
						})
					]
				})
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-2 lg:grid-cols-4 gap-4",
				children: [
					showUsers ? /* @__PURE__ */ jsx(MetricCard, {
						icon: Users,
						label: "Total Managed Users",
						value: String(users.length),
						delta: "Across all roles"
					}) : /* @__PURE__ */ jsx(MetricCard, {
						icon: CalendarClock,
						label: "Bookings Today",
						value: String(bookingsToday),
						delta: "Starting today"
					}),
					/* @__PURE__ */ jsx(MetricCard, {
						icon: Boxes,
						label: "Total Institutional Resources",
						value: loadingResources ? "…" : String(resources.length),
						delta: "Tracked in catalog"
					}),
					/* @__PURE__ */ jsx(MetricCard, {
						icon: PackageCheck,
						label: "Active Physical Checkouts",
						value: loadingBookings ? "…" : String(activeCheckouts),
						delta: "Currently checked out"
					}),
					/* @__PURE__ */ jsx(MetricCard, {
						icon: AlertTriangle,
						label: "Pending Booking Requests",
						value: loadingBookings ? "…" : String(pendingRequests),
						delta: "Awaiting review",
						tone: pendingRequests > 0 ? "warning" : void 0
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-1 xl:grid-cols-3 gap-4",
				children: [/* @__PURE__ */ jsxs(Card, {
					className: "xl:col-span-2 bg-card/60 backdrop-blur",
					children: [/* @__PURE__ */ jsxs(CardHeader, {
						className: "flex flex-row items-center justify-between",
						children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(CardTitle, {
							className: "text-base",
							children: "Peak Resource Utilization"
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "Hourly bookings and derived occupancy across all facilities · today"
						})] }), /* @__PURE__ */ jsx(Badge, {
							variant: "outline",
							className: "text-[10px]",
							children: "LIVE"
						})]
					}), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
						className: "h-72",
						children: /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(ComposedChart, {
							data: utilizationData,
							margin: {
								left: -10,
								right: 8,
								top: 8
							},
							children: [
								/* @__PURE__ */ jsx(CartesianGrid, {
									stroke: "oklch(1 0 0 / 0.06)",
									vertical: false
								}),
								/* @__PURE__ */ jsx(XAxis, {
									dataKey: "hour",
									stroke: "oklch(0.7 0 0)",
									fontSize: 11,
									tickLine: false,
									axisLine: false
								}),
								/* @__PURE__ */ jsx(YAxis, {
									stroke: "oklch(0.7 0 0)",
									fontSize: 11,
									tickLine: false,
									axisLine: false
								}),
								/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
									background: "oklch(0.21 0.03 160)",
									border: "1px solid oklch(0.32 0.03 160)",
									borderRadius: 8,
									fontSize: 12
								} }),
								/* @__PURE__ */ jsx(Bar, {
									dataKey: "bookings",
									fill: "oklch(0.62 0.16 155 / 0.7)",
									radius: [
										6,
										6,
										0,
										0
									],
									barSize: 18
								}),
								/* @__PURE__ */ jsx(Line, {
									type: "monotone",
									dataKey: "occupancy",
									stroke: "oklch(0.82 0.17 85)",
									strokeWidth: 2.5,
									dot: false
								})
							]
						}) })
					}) })]
				}), /* @__PURE__ */ jsxs(Card, {
					className: "bg-card/60 backdrop-blur",
					children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
						className: "text-base",
						children: "Allocation by Category"
					}), /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground",
						children: "Share of booked hours by resource type"
					})] }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
						className: "h-72",
						children: categoryData.length === 0 ? /* @__PURE__ */ jsx("div", {
							className: "h-full flex items-center justify-center text-xs text-muted-foreground",
							children: "No booking activity yet"
						}) : /* @__PURE__ */ jsx(ResponsiveContainer, { children: /* @__PURE__ */ jsxs(PieChart, { children: [/* @__PURE__ */ jsx(Pie, {
							data: categoryData,
							dataKey: "value",
							nameKey: "name",
							innerRadius: 55,
							outerRadius: 90,
							paddingAngle: 3,
							children: categoryData.map((d) => /* @__PURE__ */ jsx(Cell, {
								fill: d.color,
								stroke: "none"
							}, d.name))
						}), /* @__PURE__ */ jsx(Legend, {
							iconType: "circle",
							wrapperStyle: { fontSize: 11 }
						})] }) })
					}) })]
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-1 xl:grid-cols-2 gap-4",
				children: [/* @__PURE__ */ jsxs(Card, {
					className: "bg-card/60 backdrop-blur",
					children: [/* @__PURE__ */ jsxs(CardHeader, {
						className: "flex flex-row items-center justify-between",
						children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs(CardTitle, {
							className: "text-base flex items-center gap-2",
							children: [/* @__PURE__ */ jsx(Wrench, { className: "h-4 w-4 text-warning" }), " Maintenance Registry"]
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "Resources currently under maintenance"
						})] }), /* @__PURE__ */ jsxs(Badge, {
							variant: "outline",
							className: "text-[10px]",
							children: [maintenanceItems.length, " OPEN"]
						})]
					}), /* @__PURE__ */ jsx(CardContent, {
						className: "pt-0",
						children: maintenanceItems.length === 0 ? /* @__PURE__ */ jsx("p", {
							className: "text-center text-xs text-muted-foreground py-8",
							children: "Nothing under maintenance right now."
						}) : /* @__PURE__ */ jsx("ul", {
							className: "divide-y divide-border",
							children: maintenanceItems.map((m) => /* @__PURE__ */ jsxs("li", {
								className: "py-3 flex items-center justify-between gap-3",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "min-w-0",
									children: [/* @__PURE__ */ jsxs("p", {
										className: "text-sm font-medium truncate",
										children: [
											m.name,
											" · ",
											m.location
										]
									}), /* @__PURE__ */ jsx("p", {
										className: "text-[11px] text-muted-foreground",
										children: m.maintenanceNote || "No note provided"
									})]
								}), /* @__PURE__ */ jsx(Badge, {
									variant: "outline",
									className: "border-warning/40 text-warning",
									children: "Under maintenance"
								})]
							}, m._id))
						})
					})]
				}), showAudit && /* @__PURE__ */ jsxs(Card, {
					id: "audit",
					className: "bg-card/60 backdrop-blur",
					children: [/* @__PURE__ */ jsxs(CardHeader, {
						className: "flex flex-row items-center justify-between",
						children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs(CardTitle, {
							className: "text-base flex items-center gap-2",
							children: [/* @__PURE__ */ jsx(Activity, { className: "h-4 w-4 text-success" }), " Audit Engine Terminal"]
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "Live infrastructure & security feed"
						})] }), /* @__PURE__ */ jsx(Badge, {
							variant: "outline",
							className: "text-[10px] border-success/40 text-success",
							children: "● STREAMING"
						})]
					}), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsx("div", {
						className: "rounded-lg border border-border bg-background/60 font-mono text-[11px] leading-relaxed p-3 max-h-72 overflow-auto",
						children: auditRows.length === 0 ? /* @__PURE__ */ jsx("p", {
							className: "text-muted-foreground py-4 text-center",
							children: "No recent activity."
						}) : auditRows.map((row, i) => /* @__PURE__ */ jsxs("div", {
							className: "flex gap-3 py-0.5",
							children: [
								/* @__PURE__ */ jsx("span", {
									className: "text-muted-foreground shrink-0",
									children: row.t
								}),
								/* @__PURE__ */ jsx("span", {
									className: "shrink-0 w-[90px] font-semibold text-primary",
									children: row.a
								}),
								/* @__PURE__ */ jsx("span", {
									className: "shrink-0 w-[110px] text-muted-foreground truncate",
									children: row.who
								}),
								/* @__PURE__ */ jsx("span", {
									className: "text-foreground/90 truncate",
									children: row.msg
								})
							]
						}, i))
					}) })]
				})]
			})
		]
	})] });
}
function MetricCard({ icon: Icon, label, value, delta, trend = "up", tone }) {
	return /* @__PURE__ */ jsx(Card, {
		className: `bg-card/60 backdrop-blur ${tone === "warning" ? "ring-1 ring-warning/40" : ""}`,
		children: /* @__PURE__ */ jsxs(CardContent, {
			className: "p-5",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex items-center justify-between",
					children: [
						/* @__PURE__ */ jsx("div", {
							className: `h-9 w-9 rounded-lg flex items-center justify-center ${tone === "warning" ? "bg-warning/15 text-warning" : "bg-primary/15 text-primary"}`,
							children: /* @__PURE__ */ jsx(Icon, { className: "h-4 w-4" })
						}),
						tone !== "warning" && /* @__PURE__ */ jsx("span", {
							className: "text-[11px] inline-flex items-center gap-0.5 text-muted-foreground",
							children: delta
						}),
						tone === "warning" && /* @__PURE__ */ jsx("span", {
							className: "text-[11px] inline-flex items-center gap-0.5 text-warning",
							children: delta
						})
					]
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-4 text-2xl font-semibold tracking-tight",
					children: value
				}),
				/* @__PURE__ */ jsx("p", {
					className: "text-[11px] text-muted-foreground mt-0.5",
					children: label
				})
			]
		})
	});
}
function FacultyDashboard() {
	const { user } = useRole();
	const { data: bookings = [], isLoading: loadingBookings } = useQuery({
		queryKey: ["bookings", "mine"],
		queryFn: () => getBookings()
	});
	const { data: courses = [], isLoading: loadingCourses } = useQuery({
		queryKey: ["courses", "all"],
		queryFn: getCourses
	});
	const { data: attendance = [] } = useQuery({
		queryKey: ["attendance", "mine"],
		queryFn: getAttendance
	});
	const myCourses = courses.filter((c) => c.lecturer?._id === user?._id || c.lecturer === user?._id);
	const studentIds = /* @__PURE__ */ new Set();
	myCourses.forEach((c) => (c.students ?? []).forEach((s) => studentIds.add(typeof s === "string" ? s : s._id)));
	const upcoming = bookings.filter((b) => ["pending", "approved"].includes(b.status) && b.startTime && new Date(b.startTime).getTime() >= Date.now()).sort((a, b) => new Date(a.startTime).getTime() - new Date(b.startTime).getTime()).slice(0, 6);
	let present = 0, total = 0;
	attendance.forEach((session) => {
		(session.records ?? []).forEach((r) => {
			total += 1;
			if (r.status === "present") present += 1;
		});
	});
	const attendancePct = total ? Math.round(present / total * 100) : null;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "Faculty Workspace",
		subtitle: "Resource bookings and academic logbook at a glance"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-5 lg:px-8 py-6 space-y-6",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-2 lg:grid-cols-4 gap-4",
			children: [
				/* @__PURE__ */ jsx(MetricCard, {
					icon: CalendarClock,
					label: "My Upcoming Bookings",
					value: loadingBookings ? "…" : String(upcoming.length),
					delta: "Pending or approved"
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: BookOpen,
					label: "Active Courses",
					value: loadingCourses ? "…" : String(myCourses.length),
					delta: "Currently teaching"
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: GraduationCap,
					label: "Students Under Review",
					value: loadingCourses ? "…" : String(studentIds.size),
					delta: "Enrolled across your courses"
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: CheckCircle2,
					label: "Attendance Logged",
					value: attendancePct === null ? "—" : `${attendancePct}%`,
					delta: "All sessions to date",
					trend: "up"
				})
			]
		}), /* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-1 xl:grid-cols-3 gap-4",
			children: [/* @__PURE__ */ jsxs(Card, {
				className: "xl:col-span-2 bg-card/60",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "My Upcoming Resource Bookings"
				}) }), /* @__PURE__ */ jsx(CardContent, {
					className: "pt-0",
					children: loadingBookings ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-6 text-center",
						children: "Loading…"
					}) : upcoming.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-6 text-center",
						children: "No upcoming bookings."
					}) : /* @__PURE__ */ jsx("ul", {
						className: "divide-y divide-border",
						children: upcoming.map((b) => /* @__PURE__ */ jsxs("li", {
							className: "py-3 flex items-center justify-between gap-3",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "min-w-0",
								children: [/* @__PURE__ */ jsx("p", {
									className: "text-sm font-medium truncate",
									children: b.resource?.name ?? "—"
								}), /* @__PURE__ */ jsxs("p", {
									className: "text-[11px] text-muted-foreground",
									children: [
										b.purpose ?? "—",
										" · ",
										fmtWindow(b)
									]
								})]
							}), /* @__PURE__ */ jsx(Badge, {
								variant: b.status === "pending" ? "outline" : "secondary",
								className: b.status === "pending" ? "border-warning/40 text-warning" : "border-success/40 text-success",
								children: b.status === "pending" ? "Pending" : "Confirmed"
							})]
						}, b._id))
					})
				})]
			}), /* @__PURE__ */ jsxs(Card, {
				className: "bg-card/60",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Quick Actions"
				}) }), /* @__PURE__ */ jsx(CardContent, {
					className: "space-y-2",
					children: [
						{
							label: "Open Master Schedule",
							href: "/app/schedule"
						},
						{
							label: "Log today's attendance",
							href: "/app/logbook"
						},
						{
							label: "Update marks",
							href: "/app/results"
						},
						{
							label: "Request a resource",
							href: "/app/schedule"
						}
					].map((a) => /* @__PURE__ */ jsx("a", {
						href: a.href,
						className: "block w-full text-left px-3 py-2.5 rounded-lg border border-border bg-background/40 hover:bg-accent text-sm",
						children: a.label
					}, a.label))
				})]
			})]
		})]
	})] });
}
function StudentDashboard() {
	const { user } = useRole();
	const { data: bookings = [], isLoading: loadingBookings } = useQuery({
		queryKey: ["bookings", "mine"],
		queryFn: () => getBookings()
	});
	const { data: results = [], isLoading: loadingResults } = useQuery({
		queryKey: ["results", "mine"],
		queryFn: getResults
	});
	const { data: attendance = [], isLoading: loadingAttendance } = useQuery({
		queryKey: ["attendance", "mine"],
		queryFn: getAttendance
	});
	const activeBookings = bookings.filter((b) => [
		"pending",
		"approved",
		"checked_out"
	].includes(b.status));
	const borrowedAssets = bookings.filter((b) => b.status === "checked_out" && ["book", "equipment"].includes(b.resource?.type));
	const soonestDue = borrowedAssets.map((b) => b.dueDate ? new Date(b.dueDate) : null).filter((d) => !!d).sort((a, b) => a.getTime() - b.getTime())[0];
	let present = 0, total = 0;
	attendance.forEach((session) => {
		const mine = (session.records ?? []).find((r) => (r.student?._id ?? r.student) === user?._id);
		if (mine) {
			total += 1;
			if (mine.status === "present") present += 1;
		}
	});
	const attendancePct = total ? Math.round(present / total * 100) : null;
	const avgScore = results.length ? Math.round(results.reduce((s, r) => s + (r.finalScore ?? 0), 0) / results.length) : null;
	const byCourse = /* @__PURE__ */ new Map();
	results.forEach((r) => {
		const id = r.course?._id ?? r.course;
		const name = r.course?.title ? `${r.course.courseCode ?? ""} · ${r.course.title}`.replace(/^ · /, "") : "Course";
		byCourse.set(id, {
			name,
			grade: Math.round(r.finalScore ?? 0),
			present: 0,
			total: 0
		});
	});
	attendance.forEach((session) => {
		const courseId = session.course?._id ?? session.course;
		const mine = (session.records ?? []).find((r) => (r.student?._id ?? r.student) === user?._id);
		if (!mine || !byCourse.has(courseId)) return;
		const entry = byCourse.get(courseId);
		entry.total += 1;
		if (mine.status === "present") entry.present += 1;
	});
	const courseProgress = Array.from(byCourse.values());
	const today = bookings.filter((b) => (isToday(b.startTime) || isToday(b.dueDate)) && [
		"pending",
		"approved",
		"checked_out"
	].includes(b.status)).slice(0, 5);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(AppHeader, {
		title: "My Campus",
		subtitle: "Personal bookings, borrowed assets and academic progress"
	}), /* @__PURE__ */ jsxs("div", {
		className: "px-5 lg:px-8 py-6 space-y-6",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-2 lg:grid-cols-4 gap-4",
			children: [
				/* @__PURE__ */ jsx(MetricCard, {
					icon: CalendarClock,
					label: "My Active Bookings",
					value: loadingBookings ? "…" : String(activeBookings.length),
					delta: "Pending, approved or out"
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: Boxes,
					label: "Borrowed Assets",
					value: loadingBookings ? "…" : String(borrowedAssets.length),
					delta: soonestDue ? `Next due ${soonestDue.toLocaleDateString()}` : "None due",
					tone: soonestDue ? "warning" : void 0
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: CheckCircle2,
					label: "Attendance Rate",
					value: attendancePct === null ? "—" : `${attendancePct}%`,
					delta: "All sessions to date",
					trend: "up"
				}),
				/* @__PURE__ */ jsx(MetricCard, {
					icon: GraduationCap,
					label: "Average Score",
					value: avgScore === null ? "—" : `${avgScore}%`,
					delta: "Across graded courses",
					trend: "up"
				})
			]
		}), /* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-1 xl:grid-cols-3 gap-4",
			children: [/* @__PURE__ */ jsxs(Card, {
				className: "xl:col-span-2 bg-card/60",
				children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Course Progress"
				}), /* @__PURE__ */ jsx("p", {
					className: "text-xs text-muted-foreground",
					children: "Continuous assessment & attendance per course"
				})] }), /* @__PURE__ */ jsx(CardContent, {
					className: "space-y-4",
					children: loadingResults || loadingAttendance ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-6 text-center",
						children: "Loading…"
					}) : courseProgress.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-6 text-center",
						children: "No graded courses yet."
					}) : courseProgress.map((c) => /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("div", {
						className: "flex items-center justify-between mb-1.5",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm font-medium",
							children: c.name
						}), /* @__PURE__ */ jsxs("span", {
							className: "text-xs text-muted-foreground",
							children: [
								"grade ",
								c.grade,
								"% ",
								c.total > 0 ? `· attendance ${Math.round(c.present / c.total * 100)}%` : ""
							]
						})]
					}), /* @__PURE__ */ jsx(Progress, {
						value: c.grade,
						className: "h-1.5"
					})] }, c.name))
				})]
			}), /* @__PURE__ */ jsxs(Card, {
				className: "bg-card/60",
				children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Today"
				}) }), /* @__PURE__ */ jsx(CardContent, {
					className: "space-y-3",
					children: loadingBookings ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-4 text-center",
						children: "Loading…"
					}) : today.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground py-4 text-center",
						children: "Nothing scheduled today."
					}) : today.map((b) => /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-3 rounded-lg border border-border bg-background/40 px-3 py-2.5",
						children: [/* @__PURE__ */ jsx("div", {
							className: "text-xs font-mono text-primary w-12",
							children: b.startTime ? new Date(b.startTime).toLocaleTimeString(void 0, {
								hour: "2-digit",
								minute: "2-digit"
							}) : "Due"
						}), /* @__PURE__ */ jsxs("div", {
							className: "text-sm",
							children: [b.resource?.name ?? "—", b.purpose ? ` · ${b.purpose}` : ""]
						})]
					}, b._id))
				})]
			})]
		})]
	})] });
}
//#endregion
export { Dashboard as component };
